# ScriptGenerator（脚本构建器）项目启动文档

> 版本：v1.2
> 日期：2026-09-07
> 状态：待批准启动（M0 Spike 先行，通过后进入 M1）
> 依据：《项目分析文档.md》v0.26 —— 本文档只做启动决策与行动规划，不重复展开设计细节，引用处标注分析文档章节

---

## 1. 项目一句话

> 一个**面向所有人**的 Windows 桌面自动化脚本构建器：用户只需"截图目标、选择动作、增加条件、增加循环"，就能把重复的电脑操作做成可一键运行、可验证结果、可导出给 Agent 的脚本，全程不接触任何 RPA/编程概念（分析文档 §1.1）。

## 2. 启动依据摘要（结论：有条件 GO）

| 维度 | 结论 | 依据 |
|---|---|---|
| 合理性 | 问题真实、方案与问题匹配、失败处理闭环完整 | 分析文档 §1~§6、§14 |
| 技术可行性 | 无"不可能"项；复用优先原则把自研风险压至最低 | §10.4、§16.1 |
| 决策完备性 | 9 项关键决策全部定案，无悬而未决项 | §16.1（见第 3 节） |
| 剩余未知 | 唯一实质赌注：**真实软件上的部件定位命中率**——属经验问题，用 M0 Spike 裁决，失败代价可忽略（M0 可丢弃） | §15 M0、第 8 节 |

**启动方式：批准启动项目，但 M1 的入场券 = M0 验证达标（Go/No-Go 标准见第 8 节）。**

## 3. 定案决策清单（分析文档 §16.1 #1~#9）

| # | 决策 | 结论 |
|---|---|---|
| 1 | 目标平台 | ✅ 仅 Windows |
| 2 | AI 运行方式 | ✅ 默认云端 API；本地模型为隐私/离线选项 |
| 3 | OCR 方式 | ✅ 本地 RapidOCR（已安装 3.8.1） |
| 4 | 录制模式优先级 | ✅ M3（先验证手动搭建闭环） |
| 5 | 脚本导出执行模式 | ✅ 确定性封装（A）为主，LLM 编排（B）作进阶 |
| 6 | 应用形态/GUI | ✅ Electron + Web 节点图库（LogicFlow 推荐，落地前 demo 定） |
| 7 | AI 增强引入节奏 | ✅ M3 才上 |
| 8 | 预期结果校验（L2） | ✅ MVP 就做 |
| 9 | 目标存储 | ✅ MVP 内嵌（JSON / WIDGETS），V2 外链 |

配套原则：**复用优先，自研延后**（§10.4）——底层一律用成熟 Python 包与现成图库，自研只限产品逻辑本身（选区交互、解释器、结果判定器、导出器）。

## 4. 项目范围

### 4.1 做（MVP 链路）

1. **截图目标（双截图）**：先框操作页面，再框页面内有效部件（点击范围），自动采集 UI 树/形状/文字/页内坐标（§4.3、§5.1）
2. **选择动作**：单击/双击/输入文本/等待/提示我/快捷键（MVP 六动作，§5.2）
3. **增加条件**：如果看到/没看到 X → 就…否则…（§5.3）
4. **增加循环**：重复 N 次 / 直到看到 X（§5.4）
5. **预期结果校验（L2）**：做完后应该看到 X，失败走重试/提示/停止/跳过（§6.2~6.3）
6. **运行引擎**：三级定位（UI 树 → 部件相似度 → 页面内坐标）+ 页面内执行（§7.3）
7. **脚本 JSON**：单文件内嵌存储、可分享（§11）

### 4.2 不做（明确边界，§3.3）

- ❌ 不暴露选择器/xpath/控件树/坐标等术语与参数
- ❌ 不做变量、表达式、函数调用（MVP）
- ❌ 不做进程注入/内存读写（仅屏幕级模拟）
- ❌ 不做跨平台
- ❌ 不自研 OCR/图引擎/识别模型（复用优先）
- ❌ 录制模式（M3）、AI 增强（M3）、节点图画布（随 M1+ 渐进）、双导出出口（M4）——不阻塞 MVP

### 4.3 后期（最终版功能，预先设计到位）

- 录制模式：自动把操作拆成"积木"（步骤节点）（§12.2）
- AI 增强：视觉定位、结果语义判定、一句话生成脚本（§7.4）
- 双导出出口：图片脚本（Skill+WIDGETS）与 AI 代码脚本（§8.9）
- 目标 Agent 目录适配与自动写入（§8.6）

## 5. 技术栈定案

```
Electron 桌面壳（TypeScript/React）
  · 节点图画布：LogicFlow（LogicFlow/LiteGraph/Rete 落地前 demo 定）
  · 双截图选区覆盖层：透明无边框 BrowserWindow
        │
        │  本地 IPC（JSON-RPC，契约方法清单见《项目开发过程文档》§6.1）
        ▼
Python 引擎进程（独立进程，可 CLI 单独运行与测试）
  · mss 截图 · RapidOCR · OpenCV 相似度（形状模板）
  · uiautomation（UI 树）· pywin32 + pynput（SendInput）
  · 脚本解释器 · 结果判定器（D1/D2/D3）· 双出口导出器
        │
        ▼
云端 AI API（M3 起启用：视觉定位 / 语义判定 / 代码转译；
           默认云端、可切本地；上传需显式授权）
```

要点（§10.1~10.4）：引擎 Python；UI Web(Electron)；截图/OCR/UIA/输入/图库全部复用；脚本与导出物一律 JSON/内嵌数据。

## 6. 里程碑与交付物（§15）

| 阶段 | 内容 | 交付物 | 退出标准 |
|---|---|---|---|
| **M0** | 技术验证 Spike（可丢弃） | 命令行验证脚本 + 命中率统计 | 见第 8 节 Go/No-Go |
| **M1** | MVP 手动搭建版 | Electron+Python 可运行 App：双截图/列表式编辑器/六动作/条件/循环/L1+L2/JSON 存取 | **验收：零编程用户在 5 分钟内搭出"自动登录"脚本（含失败提示）并成功运行** |
| **M2** | 可靠性增强 | 相似度融合调优、UI 树路径消歧、跨机/换分辨率实测 | 定位命中率与稳定性达标（《项目开发过程文档》第 8 节指标） |
| **M3** | AI 增强 + 录制模式 | 可插拔 AI（默认云端）、结果语义判定、录制"自动拆积木" | 录制脚本可在真实案例库回归通过 |
| **M4** | 导出 + 自然语言 + 双出口 | 图片脚本导出（目录适配自动写入）、AI 代码脚本转译、一句话生成 | 双出口在真实 Agent 目录可用 |

## 7. 资源与团队

| 项 | 建议 |
|---|---|
| 角色 | Python 引擎开发（1）+ Electron 前端开发（1）为主；单人可串行推进（先引擎后 UI），联调在 M1 中段 |
| 工具链 | Git（代码与脚本 JSON 均纳入版本管理）、Python 3.x + uv/pip、Node + Electron 工具链、PyInstaller（M1 末评估引擎侧打包） |
| 仓库布局 | `engine/`（Python 进程与 CLI 冒烟）、`app/`（Electron UI）、`docs/`（三份文档 + 变更记录）、`smoke/`（可丢弃验证脚本，M0 产物） |
| 外部依赖 | RapidOCR(3.8.1 已装)、云端 AI 账号（M3 前申请并实测计费） |

## 8. 启动门禁：M0 Spike 与 Go/No-Go 标准

**M0 目标（1~2 周，产物可丢弃）：回答四个杀局问题。**

| # | 验证问题 | 方法 | 通过线 |
|---|---|---|---|
| ① | 双截图闭环定位命中率 | 对 5 类真实目标（网页登录/ERP 网页/桌面客户端/图标按钮软件/动态内容界面）各 ≥100 次页面定位+部件定位 | 常规场景 ≥90%；无 UIA 场景 ≥80% |
| ② | UIA 覆盖率实测 | uiautomation 扫描 10 个常见软件，统计 UI 树可命中比例 | 记录数据即可（决定①级角色与宣传口径） |
| ③ | 云端 AI 视觉质量/延迟 | 真实截图测 grounding + 语义判定准确率与往返延迟 | 记录数据；识别准确率 ≥85% 为 M3 信心线 |
| ④ | Electron↔Python 原型 | 选区层交互 + IPC 一次往返（截图→识别→坐标回传） | 往返 <1s、坐标无错位（含 125%/150% DPI） |

**判定**：①②③④ 通过 → **GO，启动 M1（约 2~3 个月）**；①不达标但可定向修复（如网页/标准控件类达标）→ **有条件 GO**，首版收敛目标应用类别后扩展；①普遍 <60% 且无收敛迹象 → **NO-GO**，转向特定应用绑定或中止。

## 9. 关键风险与应对（§14 摘要）

| 风险 | 等级 | 应对 |
|---|---|---|
| 定位命中率不足（界面改版/无 UIA） | 🔴 | M0 量化；三级降级 + L1 重试 + 提示我兜底；M2 融合调优 |
| 结果级失败未被察觉 | 🔴 | L2 预期校验（MVP 就有）+ D1~D3 判定器 + M3 语义判定 |
| 引擎进程/Electron IPC 稳定性 | 🟡 | M0 原型验证；契约先行（JSON-RPC schema 版本化） |
| 录制"自动拆积木"复杂度 | 🟡 | 推迟 M3；先手动闭环验证产品价值 |
| 云端 AI 隐私/成本 | 🟡 | 显式授权 + 知情提示（§7.5）；可切本地模型 |
| 打包/杀软误报 | 🟢 | M1 末评估签名与白名单流程 |

## 10. 项目纪律约定

1. **术语强制**：界面/日志/报错只用《项目分析文档》§17 术语表左列白话（如"点一下、做完后应该看到"）；内部文档可用技术词。
2. **分析先行**：任何新需求/设计变更先修订《项目分析文档》并登记变更记录，再进入开发（流程见《项目开发过程文档》第 5 节）。
3. **决策登记**：新拍板的决策写入分析文档 §16.1 已确认表（含编号），正文引用一律用"16.1 #N"。
4. **复用红线**：出现"自研 XX"提议时，先回答 10.4 的两个问题（有无现成包？数据是否证明瓶颈？）。

## 11. 启动后 30 天行动计划

| 周 | 行动 | 输出 |
|---|---|---|
| W1 | M0①：搭最小闭环 CLI（页面定位 → 双截图部件三级定位 → SendInput 点击）；并行做 UIA 覆盖率扫描② | M0 脚本 + 命中率原始数据 |
| W2 | M0③④：接云端 AI 接口测质量延迟；Electron 空壳 + IPC 原型 + 选区层 | 四问题数据表 + GO/NO-GO 判定 |
| W3 | （若 GO）M1 启动：仓库初始化、引擎 CLI 版解释器 + JSON 模型 + L1/L2 骨架 | 可跑通"脚本 JSON → 执行"的命令行 MVP |
| W4 | M1 前半：Electron UI 雏形（列表式编辑器 + 双截图选区向导接入引擎） | 端到端冒烟：搭脚本→运行 |

---

## 附：本文档修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-07 | 初稿：依据《项目分析文档》v0.26 输出启动决策、范围、技术栈、里程碑、M0 门禁与 30 天计划 |
| v1.1 | 2026-09-07 | 自查修正：技术栈图改树形（消除 CJK 等宽错位）；M2 行引用补书名号；W1 行动文案补"页面定位→双截图部件三级定位" |
| v1.2 | 2026-09-07 | 终检修正：§4.1 标题去"四操作"（清单为 MVP 能力非仅四操作）；§7 工具链语病修正 |
