# ScriptGenerator（脚本构建器）项目开发过程文档

> 版本：v1.2
> 日期：2026-09-07
> 状态：随 M0 启动生效
> 依据：《项目分析文档.md》v0.26（设计权威，本文档只定义"怎么开发"，不重复设计细节）

---

## 1. 目的与适用范围

本文档定义项目从 M0 到 M4 的**开发流程、门禁标准、工程约束与质量要求**，适用于：
- 引擎开发（Python）、界面开发（Electron/TypeScript）与联调；
- 需求/设计变更的评审与落地路径；
- 每个里程碑的进出判定与验收。

文档体系约定：**《项目分析文档》回答"做什么、为什么"，本文档回答"怎么开发、怎么算完成"，《项目启动文档》回答"何时启动、资源与门禁"。**

## 2. 总体开发流程（门禁制）

```
评审/需求
  → 修订《项目分析文档》（设计先行，登记变更记录）
  → 里程碑规划（M0~M4，见第 3 节进出标准）
  → 迭代开发（2 周节奏，见第 4 节）
  → 测试与指标（见第 8 节）
  → 里程碑门禁评审（DoD 全绿才进下一阶段）
```

原则：
1. **分析先行**：任何影响行为/数据格式/术语的改动，先改分析文档并升版本，再写代码；
2. **复用优先**：不自研已有成熟替代品（红线检查见《项目启动文档》§10）；
3. **引擎可独立**：Python 引擎必须能脱离 UI 以 CLI/测试方式运行，UI 只是客户端之一；
4. **白话可测**：验收与演示都以"用户白话"表述（"5 分钟搭自动登录"），不以代码行数衡量。

## 3. 里程碑进出标准

### M0 — 技术验证（Spike）
- 进入：无前置；产出物明确允许丢弃。
- 退出（Go/No-Go）：四个杀局问题数据齐备，对照《项目启动文档》§8 通过线判定。
- DoD：① 五类目标命中率统计表；② UIA 覆盖率记录；③ 云端 AI 质量/延迟记录；④ IPC+选区原型冒烟通过（DPI 125%/150% 各一次）。
- **✅ 完成（2026-09-09）**：①③④ DoD 达成（① 常规 98.7%/无 UIA 97.5%、③ 云端 AI 实测登记——
  智谱 glm-4.6v：文字识别 100%、语义判定 83%、纯 VLM 像素定位不可用 → M3 架构“本地坐标+云端语义”、
  ④ 452ms/1px@125%）；② UIA 覆盖率记录完成；150% 与其余补测项依用户决定延后/物理机补测，
  已在《项目启动文档》§8.1 登记；判定建议 GO（带收敛清单），门禁判定结论与数据位置见《项目启动文档》§8.1
  与分析文档 §15 M0 节。注：M0 Spike 工具链保留在 `smoke/`（可丢弃；其中闭环/统计/自愈脚本在 M1 冒烟清单迁移前仍可复用）。

### M1 — MVP（手动搭建版）
- 进入：M0 判定 GO（或有条件 GO 的范围收敛清单）。
- 范围：双截图 + 六动作 + 条件/循环 + L1/L2 + JSON 存取 + Electron 列表式编辑器（节点图画布不阻塞 MVP）。
- DoD：
  1. 端到端可用：搭脚本 → 运行 → 失败提示全链路走通；
  2. **验收标准**：一名零编程用户（非开发者）在 5 分钟内搭出"自动登录"脚本（含一次故意输错密码 → 正确触发"提示我"），并成功运行；
  3. 单步部件定位耗时 ≤500ms（OCR 可异步缓存）；
  4. 脚本 JSON 可保存/加载，含 Target 全量字段；
  5. 变更记录与术语检查通过（界面文案无技术词泄漏）。

### M2 — 可靠性增强
- 范围：相似度融合权重调优、UI 树路径消歧、页面坐标降级完善、L3 显式条件、脚本导入导出。
- DoD：真实案例库（≥5 个脚本 × 各 50 次运行）命中率 ≥90%；跨机/换分辨率验证一次；误报率记录在案。

### M3 — AI 增强 + 录制模式
- 范围：AI 视觉定位（默认云端）、D3 语义判定、录制"自动拆积木"、可插拔 AI 开关。
- DoD：录制一段 ≥10 步的真实操作 → 生成脚本在案例库回归通过率 ≥85%；AI 上传前授权流程可演示；切本地模型选项可用。

### M4 — 导出 + 自然语言 + 双导出出口
- 范围：图片脚本导出（目标 Agent 目录适配与自动写入）、AI 代码脚本转译、一句话生成脚本。
- DoD：导出产物在真实 Agent 目录加载运行通过；AI 代码脚本转译产物通过引用完整性校验；单向导出标注正确。

## 4. 迭代节奏与任务管理

- 节奏：2 周一个迭代；迭代末做一次**门禁快照**（DoD 进展 + 指标 + 风险更新）。
- 任务单字段：编号 / 类型（引擎·UI·联调·文档）/ 关联设计章节（如 §5.1 双截图）/ 验收描述（白话）/ 预计与实耗。
- 文档同步：凡改到"用户可见行为、JSON 结构、术语、决策"，任务单必须带"分析文档修订"项。
- 冒烟清单常驻：`smoke/` 目录下保留最小闭环脚本（截图→识别→点击→判定），每次改动后必跑。

## 5. 需求与设计变更流程

```
用户/评审提出意见
  → 判断影响面：行为？数据格式？术语？决策？
  → 修订《项目分析文档》对应章节（升版本号，追加变更记录行）
  → 涉及决策 → 更新 §16.1 已确认表（16.1 #N 编号，正文引用统一用 #N）
  → 更新本文档或《项目启动文档》中受影响的流程/范围条目
  → 进入任务队列
```

红线：不经过上述流程直接改实现、或让正文与决策表/术语表脱节，视为流程违规（评审时打回）。

## 6. 架构与工程约束

### 6.1 分层与边界（分析文档 §9）

| 层 | 职责 | 禁止 |
|---|---|---|
| Electron UI | 编辑器/选区交互/运行面板/日志 | 不得直接调 OS 识别 API（统一走 IPC 契约） |
| IPC 契约（JSON-RPC） | 方法清单：`page.find / widget.locate / window.find / window.activate / input.click / type / verify / script.run / script.stop` | 契约变更需版本化并同步 UI/引擎两侧 |
| **契约正本** | `docs/M1_IPC契约.md` v0.1（方法/事件/错误码/典型序列；UI 与引擎两侧共用） | 改契约先改此文件，再改两侧实现与测试 |
| Python 引擎 | 截图/OCR/UIA/相似度/坐标/解释器/判定器/导出器 | 不得内嵌 UI 逻辑；可 CLI 独立运行 |
| 数据层 | `.sgscript.json`（MVP 内嵌）/ 导出 WIDGETS | 字段命名以 §11 为准，加字段必须升脚本版本 |

### 6.2 复用红线检查表（提议自研前必答）

1. 现成包/库（mss、RapidOCR、OpenCV、uiautomation、pywin32、pynput、Electron、LogicFlow、registry.py…）是否已覆盖需求？
2. 若覆盖不足：有数据证明这是瓶颈吗（命中率/延迟/用户流失）？
3. 自研范围是否属于"产品逻辑本身"（选区交互/解释器/判定器/导出器）？
→ 三个问题都指向自研，才允许立项自研，否则走现成方案。

### 6.3 引擎与脚本约定

- 脚本模型：`Script{version,name,steps}`；步骤类型 action/condition/loop；`target` 含 page 引用（§11）；
- 目标存储：MVP 内嵌（base64）；导出物 WIDGETS 内嵌 `tools/screen.py`，全局去重（§8.7）；
- 运行时降级规则（§5.1）：UI 树为空直接跳过本级；相似度识别不出才用页面内坐标；全失败走 L1（重试→提示我）；
- 判定器输出契约：`{status, failure_type, matched, confidence, layer(d1/d2/d3), action_taken}`（§6.4.5）。

## 7. 代码与质量规范

| 项 | 规范 |
|---|---|
| 语言 | 引擎 Python 3.x；UI TypeScript；IPC schema 单独成文件 |
| 日志 | 双层：引擎结构化日志（JSON 行，供指标统计）；UI 白话日志（"第 2 步 ✓ 点击成功"，词汇查 §17 术语表） |
| 错误 | 引擎抛结构化错误码（page_not_found / widget_not_found / verify_failed…），UI 映射为白话提示 |
| 测试 | 引擎单元测试（解释器/判定器/导出器为纯逻辑，优先覆盖）；UI 冒烟走引擎 CLI 桩 |
| 指标埋点 | 每步定位记录 `{method(①/②/③), confidence, elapsed_ms}`，用于 M2 调优与命中率统计 |

## 8. 测试与指标门槛

| 指标 | 门槛 | 收集方式 |
|---|---|---|
| 单步部件定位耗时 | ≤500ms（OCR 可异步） | 每步埋点 |
| 案例库命中率 | ≥90%（M2 验收线；M1 起持续收集） | `smoke/bench` 批量跑 + 日志统计 |
| 结果判定误报率 | 记录在案并 ≤5% | 判定器日志抽样核对 |
| 运行中断率（无人值守 100 步） | ≤2 次人工介入 | 长跑脚本统计"提示我"次数 |

案例库（随项目积累）：自动登录、批量填表、定时签到、等待出现再操作（§2.2）+ 每个里程碑新增的真实脚本。**案例库是 M1~M4 回归的硬资产，随代码入库。**

## 9. 验收与演示（M1 模板）

演示脚本（对照 §15 M1 验收标准）：
1. 准备：浏览器登录页 + 故意错误的密码；
2. 用户（零编程背景）操作：双截图搭 4 步（输用户名/输密码/点登录/做完后应该看到"登录成功"）；
3. 运行：第一次输对 → 成功；第二次故意输错 → 预期结果未出现 → 白话提示"用户名或密码可能不对"，用户点继续/停止；
4. 判定：全程 ≤5 分钟、无技术词、无代码。

## 10. 风险登记与升级

| 风险 | 触发信号 | 升级动作 |
|---|---|---|
| 命中率低于预期 | M0/M2 指标不过线 | 缩小目标应用范围（有条件 GO）或暂停评估 |
| IPC 契约膨胀 | 方法数失控/版本频繁 | 契约评审会，回归 JSON-RPC schema 单一来源 |
| 录制复杂度超预算（M3） | 迭代连续超期 2 个 | 砍范围：先只支持"点击+输入+等待"，滚动/快捷键后补 |
| 云端 AI 成本失控 | 单用户月成本超预算线 | 用量提示 + 可切本地模型（§7.5） |
| 界面文案技术词泄漏 | 评审抽查发现 | 立即修订 UI 文案 + 术语表补条目 |

## 11. 构建与发布流程（M1 末起）

1. 引擎侧：PyInstaller 打包为独立可执行（或首次发布用 python 解释器方式）→ 冒烟全绿；
2. UI 侧：Electron 打包（NSIS 安装包）→ 安装后自检：DPI 声明、RapidOCR 模型文件路径、引擎进程拉起；
3. 杀软：提交误报白名单申请；发布前在干净虚拟机走一遍安装→建脚本→运行；
4. 版本：产品 V1 = M1+M2 达成（分析文档 §15 注的 V1 定义）；V1 发布前冻结术语表与 JSON v1.0。

## 12. 文档与知识管理

| 文档 | 责任人 | 更新时机 |
|---|---|---|
| 《项目分析文档》 | 设计与评审人 | 任何行为/格式/术语/决策变更（先行） |
| 《项目启动文档》 | 项目负责人 | 范围/资源/门禁变化 |
| 《项目开发过程文档》（本文档） | 项目负责人 | 流程/规范/门禁变化 |
| 变更记录（分析文档附录） | 所有修订者 | 每次版本修订追加一行（vX.Y、日期、要点） |

---

## 附：本文档修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-07 | 初稿：门禁制流程、里程碑进出标准（对齐分析文档 v0.26 的 M0~M4 与决策 #1~#9）、工程约束、测试指标与发布流程 |
| v1.1 | 2026-09-07 | 自查修正：案例库命中率指标行表述精确化（M2 验收线、M1 起收集） |
| v1.2 | 2026-09-07 | 终检修正：§6.1 IPC 方法清单中 `script.run\|stop` 的竖线会破坏 Markdown 表格，改为 `script.run / script.stop` |
| v1.3 | 2026-09-09 | M0 完成登记（§3 M0 DoD 更新为 ✅ 完成并注明 150%/③ 的延后登记位）；M1 进入条件保持"判定 GO（含收敛清单）" |
| v1.4 | 2026-09-09 | M0③ DoD 达成登记（§3 M0 条目更新：③ 云端 AI 实测完成、150% 与物理机补测项延后登记位） |
