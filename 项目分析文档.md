# 脚本构建器（ScriptGenerator）项目分析文档

> 版本：v0.29（分析稿）
> 日期：2026-09-07
> 状态：待评审 — 本文档只做分析，不含任何代码实现

---

## 目录

1. [项目概述](#1-项目概述)
2. [目标用户与使用场景](#2-目标用户与使用场景)
3. [产品定位与核心原则](#3-产品定位与核心原则)
4. [核心概念模型](#4-核心概念模型)
5. [四大基本操作详解](#5-四大基本操作详解)
6. [错误处理与结果校验](#6-错误处理与结果校验)
7. [目标识别的 AI 增强](#7-目标识别的-ai-增强)
8. [脚本导出为 Skill 与 Tools](#8-脚本导出为-skill-与-tools)
9. [技术架构](#9-技术架构)
10. [关键技术选型](#10-关键技术选型)
11. [数据模型（脚本格式）](#11-数据模型脚本格式)
12. [UI/UX 设计](#12-uiux-设计)
13. [非功能性需求](#13-非功能性需求)
14. [风险与挑战](#14-风险与挑战)
15. [里程碑与路线图](#15-里程碑与路线图)
16. [开放问题与待决策项](#16-开放问题与待决策项)
17. [附录：术语对照表](#17-附录术语对照表)

---

## 1. 项目概述

### 1.1 一句话定位

> 一个**面向所有人**的 Windows 桌面自动化脚本构建器：用户只需"截图目标、选择动作、增加条件、增加循环"四个操作，就能把重复的电脑操作做成一个可一键运行的脚本，全程不接触任何 RPA / 编程概念。

### 1.2 背景与动机

传统的 RPA（Robotic Process Automation，机器人流程自动化）工具（如 UiPath、影刀、按键精灵）虽然功能强大，但存在明显的使用门槛：

| 痛点 | 传统 RPA 的表现 |
|---|---|
| 术语晦涩 | "元素定位""选择器""xpath""流程变量""异常处理" 等词汇吓退普通用户 |
| 界面复杂 | 流程图 + 属性面板 + 变量面板堆叠，学习曲线陡 |
| 强依赖选择器 | 网页/桌面元素定位需要理解 DOM、控件树等底层概念 |
| 定位脆弱 | 选择器一旦失效，普通用户完全不会修 |

**本项目的核心命题**：把 RPA 里最常用的一小撮能力（找到目标 → 执行动作 → 分支 → 循环 → 验证结果），重新包装成几个连普通人都能懂的动词，把复杂度全部隐藏在"截图"这一直觉交互背后。

### 1.3 "去 RPA 化"的本质

本产品**技术上仍然是 RPA**（屏幕目标识别 + 输入模拟 + 流程控制），但**产品呈现上不是 RPA**。两者的边界如下：

| 维度 | 传统 RPA | 本项目 |
|---|---|---|
| 目标定位方式 | 选择器 / xpath / 控件树 | **截图框选** |
| 动作表达 | "点击元素" + 坐标/属性 | **"点这里"** |
| 分支表达 | "条件判断 / IF 节点" | **"如果看到…就…"** |
| 循环表达 | "For 循环 / While 循环" | **"重复几次 / 一直重复到…"** |
| 结果验证 | "断言 / 异常捕获" | **"做完后应该看到…"** |
| 用户心智 | 流程图工程师 | **搭积木 / 记下我做过的操作** |

---

## 2. 目标用户与使用场景

### 2.1 用户画像

| 画像 | 特征 | 典型诉求 |
|---|---|---|
| 行政 / 文员 | 不写代码，日常重复填报 | 批量录入 Excel、定期复制粘贴 |
| 小商家 / 运营 | 会用电脑，不愿学工具 | 定时抓取数据、批量发消息 |
| 学生 / 个人 | 动手能力强但无编程基础 | 自动签到、抢课、整理文件 |
| 初级自动化爱好者 | 用过按键精灵，觉得难用 | 想更简单、更可靠的自动化 |

> 关键假设：**用户完全不懂"元素""选择器""循环变量"这些词**，但天然理解"截图""点击""如果""重复""做完后应该看到"。

### 2.2 典型场景（验证用）

1. **自动登录**：截图"用户名框"→ 输入账号 → 截图"密码框"→ 输入密码 → 截图"登录按钮"→ 点击 → 校验"应该看到登录成功"。
2. **批量填表**：循环（重复 N 次）{ 截图下一行 → 输入数据 → 截图"保存"→ 点击 }。
3. **定时签到**：截图"签到按钮"→ 如果看到就点击，否则跳过。
4. **等待出现再操作**：一直重复直到看到"下载完成"→ 点击"下一步"。

---

## 3. 产品定位与核心原则

### 3.1 四大基本操作（唯一的对外接口）

产品的功能面**只保留四个动词**，其余能力一律不暴露：

1. **截图目标**（Screenshot Target）— 告诉脚本"要操作什么"
2. **选择动作**（Select Action）— 告诉脚本"对它做什么"
3. **增加条件**（Add Condition）— 告诉脚本"什么情况下做/不做"
4. **增加循环**（Add Loop）— 告诉脚本"要做几遍/做到什么时候"

> 补充说明：错误处理（第 6 章）和结果校验**不是第五个操作**，而是复用"截图"这个动词——"做完后应该看到 X"就是用同一套截图交互完成的，因此对外接口仍是四个动词。

### 3.2 设计原则

| 原则 | 说明 |
|---|---|
| **截图即定位** | 用户永远不输入坐标、不写选择器，唯一的目标表达方式是"双截图：先框操作页面，再框页内部件" |
| **截图即验证** | "做完后应该看到什么"同样用截图表达，结果校验零学习成本 |
| **默认值兜底** | 每个操作都预设合理默认值，用户"截图 + 选动作"两步即可完成一个步骤 |
| **术语降维** | 界面、报错、日志里出现的词，必须是日常用语（见 [附录](#17-附录术语对照表)） |
| **渐进披露** | 高级参数（匹配容差、超时、重试、AI 增强）藏在"高级设置"里，默认收起 |
| **先能用，再可靠** | MVP 优先跑通"截图→点击"主链路，鲁棒性与 AI 增强在后续版本迭代 |
| **复用优先** | 底层能力尽量用成熟 Python 包；自研推迟到项目落地、确证瓶颈之后（见 10.4） |

### 3.3 明确的"不做"清单

- ❌ 不暴露元素选择器、xpath、控件树
- ❌ 不做 RPA 式的节点类型爆炸（几十种节点 + 变量面板 + 异常处理器堆叠）；节点图只保留少数几种节点 + 自动布局，见第 12 章
- ❌ 不做变量、表达式、函数调用（至少 MVP 不做）
- ❌ 不暴露坐标系统、OCR 阈值、匹配度等技术参数
- ❌ 不做跨应用的进程注入 / 内存读写（只做屏幕级模拟）
- ❌ 不做跨平台（**已确认：仅 Windows**，见第 16 章决策 #1）

---

## 4. 核心概念模型

### 4.1 脚本（Script）

一个脚本 = 一个**有名字的有序步骤列表**，可保存、可加载、可一键运行、可分享给他人。

```
脚本 "自动登录"
  ├── 步骤1：输入 "用户名框" 文本 "admin"（截图目标 + 输入动作）
  ├── 步骤2：输入 "密码框" 文本 "••••••"（截图目标 + 输入动作）
  ├── 步骤3：点击 "登录按钮"（截图目标 + 点击动作）
  ├── 步骤4：如果看到 "验证码" 就 提示我（条件）
  └── 步骤5：一直重复直到看到 "首页" { 等待 2 秒 }（循环：轮询等待登录完成）
```

### 4.2 步骤（Step）的三种类型

脚本由三种**节点类型**组合而成，其中"条件"和"循环"内部可嵌套任意步骤：

```
步骤类型
├── 动作步骤 (Action)      —— 叶子节点，真正"干活"；可带"预期结果"
├── 条件步骤 (Condition)   —— 有 then 分支 和 else 分支
└── 循环步骤 (Loop)        —— 有 body 子步骤列表
```

### 4.3 目标（Target）

"截图目标"的产物是一个**目标对象**，它承载了"如何在屏幕上找到这个东西"的全部信息，但对用户完全隐藏。录制一个目标采用**双截图**：第一次框选确定**操作页面**（脚本操作哪个窗口/页面），第二次框选确定**页面内的有效部件**（要点的按钮、要输入的框，即点击范围）。截图时系统自动采集多路定位信号，一个目标内部包含：

| 内部字段 | 用途 | 对用户可见性 |
|---|---|---|
| 所属操作页面（Page） | 第一次截图产物：页面上下文（进程名 / 窗口标题可通配 / 类名）+ 页面截图与尺寸 + **页面坐标系**（部件坐标的参照系） | 半可见（显示页面名，可"重新截图页面"） |
| 部件范围图像（可多张） | 第二次截图产物：有效部件（点击范围）的形状模板，用于相似度匹配 + 多模板容错 | 可见（就是那张部件截图，可"再截一张"） |
| 部件文字 | 第二次截图自动 OCR 提取，同样用于相似度匹配 | 半可见（显示"已识别：登录"） |
| UI 树节点 | 第二次截图中心点 UIA hit-test：ControlType / Name / AutomationId / 父节点路径 | 不可见（自动采集） |
| 部件页面内坐标 | 第二次截图：部件范围矩形在**操作页面内**的位置 + 默认点击点（范围中心，可偏移） | 不可见（自动记录） |
| 语义描述 | AI 视觉定位用（可选） | 不可见（自动生成） |
| 匹配方式 | 运行时降级顺序：① UI 树 → ② 部件相似度 → ③ 页面内坐标（→ ④ AI 可选） | 不可见（自动判定） |
| 容差阈值 | 匹配灵敏度 | 藏在高级设置 |

---

## 5. 四大基本操作详解

### 5.1 截图目标（Screenshot Target）

**用户视角（双截图，两个动作）**：点一下"截图目标"按钮 → 屏幕变暗、出现十字光标 → **① 先框住脚本要操作的整个页面**（如浏览器窗口 / 软件主界面）→ **② 再在该页面内框住要操作的有效部件**（如"登录"按钮，即点击范围）→ 松开即完成，回显"部件缩略图 + 已识别文字"。同一页面内的后续步骤只做第 ② 步（页面自动沿用）；换到另一个页面（如登录后跳转的主页）时，重新做一次第 ① 步即可。

**系统视角（对用户隐藏）**：

1. **第一次截图（操作页面）**：截取页面图像 → 用 `WindowFromPoint` / UIA hit-test 确定窗口上下文 → 记录进程名、窗口标题（可通配）、窗口类名（确定性 WinAPI，无需 LLM）→ 建立**操作页面**对象：页面上下文 + 页面截图与尺寸 + 页面坐标系。
2. **第二次截图（有效部件）**：对用户框选的部件范围**同时做多件事**：
   - 保存范围图像作为形状模板（用于相似度匹配）；
   - 运行 OCR 提取其中文字（同样参与相似度匹配）；
   - UIA hit-test 记录该点的部件节点（ControlType / Name / AutomationId / 父链，供 UI 树定位）；
   - 记录部件范围在**页面内**的坐标（矩形 + 默认点击点 = 范围中心）。
3. 生成一个 `Target` 对象 = **页面引用 + UI 树节点 + 形状/文字 + 页面内坐标 + 建议匹配方式**。
4. 回显给用户"已选中的部件"缩略图 + 识别出的文字；点缩略图可"重新截图部件"，点页面名可"重新截图页面"。

**匹配策略（运行时，按优先级降级容错）**：

| 优先级 | 策略 | 说明 |
|---|---|---|
| ① | UI 树定位 | 先定位操作页面，再在页面 UI 树里按部件节点身份（Name/AutomationId/ControlType/父路径）定位（最可靠）；**页面/部件无 UI 树（树为空）时直接跳过本级，不算失败** |
| ② | 部件相似度 | 把"部件形状（多模板 + 多尺度）"与"部件文字（OCR）"融合打分，找与录制时最像的部件位置（**默认文字优先**、形状兜底）；**识别不出（低于置信度阈值）才落到 ③** |
| ③ | 页面内坐标 | **仅当 ② 识别不出时启用**：直接定位到录制时记录的部件页面内坐标（几何兜底，页面定位成功后即有效） |
| ④ | AI 语义定位 | ①②③ 全失败、或用户开启"AI 增强"时的语义兜底（见第 7 章，默认关闭） |

> 详见第 7 章 7.3「多策略定位器」：双截图自动采集多路信号，运行时按可靠性降级容错。

> **运行时降级规则**：上一级"明确识别不出"才进入下一级——① 在 UI 树为空时**直接跳过，不算失败**（不重试、不报错）；② 识别不出时**才**用 ③ 页面内坐标；①②③ 全失败才报"没找到 X"，走 6.2 的 L1 兜底（自动重试 → 提示我）。

> 关键设计：**结构与坐标绑定页面，形状与文字绑定部件**——先定"操作页面"（窗口上下文匹配），再在页内找部件；相似度内部默认"文字优先"（对主题/分辨率变化更鲁棒）、形状多模板兜底，`match` 字段（见第 11 章）只微调该级内的偏好。

### 5.2 选择动作（Select Action）

用户在截图目标后，从下拉/图标里选一个动作。动作清单（完整版，MVP 只做其中一部分，见下方建议）：

| 动作 | 用户表述 | 需要的参数 |
|---|---|---|
| 单击 | "点一下" | 无 |
| 双击 | "点两下" | 无 |
| 右键 | "点右键" | 无 |
| 输入文本 | "输入文字" | 文本内容 |
| 提示我 | "提醒我一下" | 提示文字（可选） |
| 拖动 | "拖到" | 目标位置（另一张截图） |
| 滚动 | "滚动" | 方向 + 距离/次数 |
| 等待 | "等一下" | 时长（秒） |
| 快捷键 | "按快捷键" | 组合键（录制捕获） |

> 说明："提示我"（暂停 + 白话弹窗，用户处理完后点"继续"）既可作为**失败处理动作**（见 6.3），也可作为**普通步骤**放进条件分支主动提醒用户（见 11 章 JSON 示例）。提示我、等待、快捷键同属"无目标"动作（不需要截图部件，如 JSON 示例中 `wait` 步骤没有 target 字段）。
>
> MVP 建议：只做 **单击、双击、输入文本、等待、提示我、快捷键** 六个，右键、拖动和滚动放 V2。

### 5.3 增加条件（Add Condition）

**用户视角**：选择一个已经截过的目标（或现截一个）→ 选择判断方向 → 系统自动生成"如果看到 X 就…否则…"。

| 条件类型（用户表述） | 运行时判断 |
|---|---|
| "如果看到 X" | 目标 X 是否出现在屏幕上 |
| "如果没看到 X" | 目标 X 是否不在屏幕上 |

条件步骤内含两个分支容器：**"就做…"（then）** 和 **"否则做…"（else）**，用户往里面放动作/条件/循环。

> 支持嵌套：条件里能放条件、循环，形成任意深度的逻辑。

### 5.4 增加循环（Add Loop）

**用户视角**：三种循环模式，用最白话的选项呈现：

| 循环模式 | 用户表述 | 参数 |
|---|---|---|
| 固定次数 | "重复 N 次" | N（数字） |
| 条件循环 | "一直重复，直到看到 X" | 目标 X |
| 无限循环 | "一直重复"（需手动停止） | 无（提供停止热键） |

循环步骤内含一个 **body 容器**，用户往里面放动作/条件/循环。

> 安全兜底：**条件循环**默认带次数上限（默认最多 1000 次，可在高级设置调整），防止"直到看到 X"永远不满足而失控；**无限循环**无次数上限，靠**全局停止热键**（默认 F12 / Esc 连按三次）+ UI 常驻"停止"按钮兜底。停止热键全局强制可用、优先级最高。

---

## 6. 错误处理与结果校验

> 本章回应核心问题：**"点击后出现错误（网络未连接、用户名/密码错误）怎么办？"**

### 6.1 先区分两类失败

自动化工具最容易忽视的一点是：**"动作执行了" ≠ "结果正确了"**。据此把失败分成两类：

| 失败类型 | 定义 | 例子 | 能否被引擎自动察觉 |
|---|---|---|---|
| **操作级失败** | 动作本身没执行成功 | 目标没找到、点击坐标越界 | ✅ 能（引擎知道） |
| **结果级失败** | 动作执行了，但结果不对 | 密码错误、网络断开、页面没跳转、弹了报错框 | ❌ 不能（引擎不知道） |

本章开头问题里的"网络未连接""用户名/密码错误"都属于**结果级失败**——工具能确认"我点了登录按钮"，但无法直接知道"登录成没成功"。解决办法是让脚本**去验证结果**。

### 6.2 三层错误处理机制（由隐到显）

#### L1 — 内置兜底（用户无感）

每个动作步骤自带的默认保护，无需用户配置：

- 目标找不到 → 自动重试 N 次（默认 3 次，间隔 1s）
- 重试仍失败 → 暂停，弹出白话提示："没找到'登录'，请确认屏幕上有没有这个按钮" → 用户点"继续/停止/跳过"

#### L2 — 预期结果校验（复用"截图"动词）

动作步骤可以**可选地**加一个"做完后应该看到什么"：

- 交互方式与"截图目标"完全一致（双截图：先框页面，再框部件），零学习成本
- 语义：`做完 [动作] 后，屏幕上应该出现 [预期结果 X]`
- 运行时：执行动作 → 短暂等待 → 检查 X 是否出现
  - 出现 → 判定成功，继续下一步
  - 没出现 → 判定失败，触发错误处理（见 6.3）

| 用户场景 | 预期结果 | 失败时的表现 |
|---|---|---|
| 登录 | 截图"登录成功/主页欢迎语" | 没出现 → 判定登录失败 |
| 点击"提交" | 截图"提交成功"弹窗 | 没出现 → 判定提交失败 |

#### L3 — 显式条件分支（进阶用户主动写）

用"增加条件"主动为已知错误写处理逻辑（四操作天然支持）：

| 用户表述 | 含义 |
|---|---|
| "如果看到'网络连接失败' → 提示我'请检查网络后点继续'" | 处理断网 |
| "如果看到'用户名或密码错误' → 停止" | 处理密码错 |
| "如果没看到'登录成功' → 重复 3 次点击登录" | 结合循环做重试 |

### 6.3 失败后可执行的动作（用户白话）

无论是 L1/L2/L3 触发的失败，都让用户选一个"失败后怎么办"：

| 失败动作 | 用户表述 | 行为 |
|---|---|---|
| 重试 | "再试一次" | 重新执行当前这一步（含其预期结果校验） |
| 提示 | "提示我" | 暂停 + 白话弹窗，用户处理后点"继续" |
| 停止 | "停止" | 结束脚本 |
| 跳过 | "跳过" | 忽略，继续下一步 |

### 6.4 结果级失败的判定方案（核心算法）

> 这是"结果级失败未被察觉"问题的直接解法：在"动作执行完"和"结果对不对"之间，补上一步**屏幕状态判定**。本节是 6.2 中 L2/L3 的实现算法。

> ⚠️ **命名区分**：本节结果判定器的层级用 **D1/D2/D3**（Detect），与 6.2 错误处理机制的 **L1/L2/L3** 是两套不同的编号，勿混淆——L 描述"失败如何被处理"，D 描述"结果如何被判定"。

#### 6.4.1 核心思路

解法：给每个"会产生结果"的动作（点击提交、登录、导出…）之后，插入一个**结果判定器（Result Verifier）**，输出结构化判定：`成功 / 失败(具体类型) / 未知`。

判定器**分层降级**，从确定性到语义化：

```
动作执行完毕
  ↓
[等待界面稳定]  0.5~2s（等弹窗/页面刷新出现）
  ↓
D1 环境探测（不截图）—— 网络连通性 / 进程存活 / 文件是否生成
  │ 探测失败 → 直接判 failure + 类型，结束（不靠"看"）
  ↓
D2 屏幕文字识别（RapidOCR）—— 截图 → OCR → 与结果特征库匹配
  ├─ 命中成功特征 → success
  ├─ 命中失败特征 → failure + 具体类型
  └─ 都没命中 → unknown
      ↓
D3 AI 语义判定（M3，可选）—— 多模态模型看屏回答"成功吗？什么错？"
      ↓
仍 unknown → 走默认策略（再试一次 / 提示我）
```

#### 6.4.2 D1 环境探测：不靠"看"来判"网络未连接"

关键洞察：**"网络未连接"是环境状态，不是界面状态**。与其等错误弹窗出现再 OCR，不如程序自己主动探测——`socket`/`urllib` 发一个轻量请求（HEAD 到已知可达地址），0.1s 内确定网络是否通。这比识别错误弹窗**可靠得多**。

| 失败类型 | 探测手段 | 说明 |
|---|---|---|
| 网络未连接 | socket / HTTP 探测 | 直接、确定、不依赖界面 |
| 目标程序崩溃 | 检查进程 PID 是否存活 | 自动化某软件时 |
| 文件未生成 | 检查预期输出文件是否存在 | 导出 / 保存类任务 |
| 剪贴板未更新 | 读剪贴板对比 | 复制类任务 |

> 原则：**凡能用环境信号确定的，就不截图识别**。截图识别是最后手段，不是首选。

#### 6.4.3 D2 屏幕文字识别：结果特征库匹配

主力方案（离线、免费、依赖已具备）。动作后截图 → RapidOCR 提取全部文字 → 与**结果特征库**匹配。

**成功与失败用不同匹配源**：

| 特征库 | 内容 | 来源 | 命中 → 判定 |
|---|---|---|---|
| 成功库 | "登录成功""欢迎""成功" + 用户截的**预期结果** | 内置 + 用户 | success |
| 失败库（分类） | 见下表 | 内置，用户无需逐个配 | failure + 类型 |
| 未知 | 都不命中 | — | unknown |

**内置失败特征库（按类型分类，可扩展）**：

| 失败类型 `type` | 匹配关键词 | 默认提示 | 默认恢复 |
|---|---|---|---|
| `auth` 认证类 | 密码、用户名、账号、登录失败、认证失败、密码错误、incorrect | "用户名或密码可能不对" | 提示我 |
| `network` 网络类 | 网络、无法连接、断网、超时、连接失败、timeout | "网络连接异常，请检查网络" | 提示我 / 再试一次 |
| `captcha` 验证码 | 验证码、图形码、captcha | "需要输入验证码" | 提示我 |
| `generic` 通用 | 错误、失败、异常、拒绝、error、failed | "操作未成功：{命中文字}" | 再试一次 |

**匹配与置信度**：多关键词命中加权，`置信度 = 命中权重 / 阈值`；低于阈值判为 unknown，避免误判。

#### 6.4.4 D3 AI 语义判定（升级项，M3 落地；详见 7.4）

MVP 的预期结果校验用"图像/文本是否出现"判断；M3 升级为**语义判定**——当 D2 判不出（unknown），或需精确区分"网络未连接 vs 密码错误 vs 验证码错误"时，用多模态模型看屏直接回答"登录成功了吗？屏幕上有什么报错？"。接口与 D2 完全一致（都是"输入屏幕 → 输出结构化判定"），只是后端换成模型；语义判定能精确区分失败类型，而不仅是"结果不对"。

#### 6.4.5 判定结果的数据结构

```json
{
  "status": "failure",
  "failure_type": "auth",
  "matched": "用户名或密码错误",
  "confidence": 0.92,
  "layer": "d2",
  "action_taken": "notify"
}
```

| 字段 | 取值 | 说明 |
|---|---|---|
| `status` | `success` / `failure` / `unknown` | 判定结果 |
| `failure_type` | `auth` / `network` / `captcha` / `generic` / `null` | 失败类型 |
| `matched` | 字符串 | 命中的文字 / 证据 |
| `confidence` | 0~1 | 置信度 |
| `layer` | `d1` / `d2` / `d3` | 哪一层判出的：D1 环境探测 / D2 OCR 识别 / D3 AI 语义 |
| `action_taken` | `retry` / `notify` / `stop` / `skip` | 后续恢复动作（见 6.3） |

#### 6.4.6 三个必须注意的工程细节

1. **等待界面稳定**：动作后界面刷新有延迟，立即截图会误判 unknown。插入 0.5~2s 稳定等待，或轮询（每隔 0.3s 截图一次，直到状态确定或超时）。
2. **判定区域**：全屏 OCR 开销大且易误匹配（状态栏、无关弹窗）。优先判定**用户框选的区域**（弹窗区、标题区），或按"最近出现的新窗口"缩小范围。
3. **避免误判**：失败关键词可能出现在无关文字里（如帮助文档"如何解决登录失败"）。对策：结合位置（弹窗/中心区域优先）+ 置信度阈值 + 多词共现。

---

## 7. 目标识别的 AI 增强

> 本章回应核心问题：**"图片目标匹配能否用 AI 识别提高准确度？"** 结论：能，而且 AI 同时解决定位与错误判定两件事。

### 7.1 先纠正一个认知：OCR 本身就是 AI

当前方案里的 **RapidOCR 就是一个深度学习模型**。所以严格说，"文字定位"这一步已经在用 AI 了。真正"非 AI"的只有 OpenCV 的**模板匹配**（经典计算机视觉，逐像素比对）。

| 技术 | 是不是 AI | 匹配逻辑 |
|---|---|---|
| OpenCV 模板匹配 | ❌ 否（经典 CV） | **像素匹配**：这片像素和模板像不像 |
| RapidOCR | ✅ 是（深度学习） | 识别出文字，再定位文字 |
| 多模态/视觉定位模型 | ✅ 是（大模型） | **语义匹配**：理解"这是登录按钮" |

### 7.2 问题的根源：像素匹配的脆弱性

模板匹配的失效，本质是它比较的是**像素**。任何视觉变化都会让匹配失败：

- 分辨率 / DPI 缩放变化 → 模板尺寸对不上
- 主题 / 深色模式切换 → 颜色全变
- 文字内容变化（如按钮上的数字、用户名）→ 像素不同
- 背景 / 布局微调 → 位置错位

**AI 的根本价值**：把"找和这张图长得一样的像素"升级为"找语义上是'登录按钮'的那个东西"。语义匹配对上述变化天然鲁棒。

### 7.3 多策略定位器：双截图 + 三级降级（推荐架构）

单一路径（截图模板匹配）准确度不够，根源是它只依赖"像素"。解法：**录制时用双截图给每个部件自动采集多路信号（页面上下文 / UI 树 / 形状 / 文字 / 页面内坐标），运行时按可靠性降级容错**——这正是专业 RPA 的做法。识别引擎做成**可插拔的降级链**：先定页面、再定部件，从结构性到像素性、从便宜到昂贵逐级尝试：

```
识别一个部件（先定页面，再定部件）
  ├── [前置] 操作页面定位  ← 第一次截图记录的页面上下文（进程名/窗口标题通配/类名）
  │                         页面找到后，其页面范围即锁定搜索边界
  ├── ① UI 树定位          ← 第二次截图 hit-test 得到的部件 UIA 节点，在页面树内按身份/路径定位；
  │                         UI 树为空（应用不暴露 UIA）→ 直接跳过本级，不算失败
  ├── ② 部件相似度定位      ← 形状（多模板 + 多尺度）+ 文字（OCR）融合打分，找"最像录的那个部件"
  │                         （默认文字优先、形状兜底）；识别不出才进入下一级
  ├── ③ 页面内坐标定位      ← 仅当 ② 识别不出时启用：落到录制时的页面内坐标（最后手段）
  └── ④ AI 语义定位（可选） ← 语义兜底；慢 / 有成本（见 7.4~7.6），默认不启用
```

**三级定位对照**：

| 优先级 | 策略 | 原理 | 可靠性 | 主要局限 |
|---|---|---|---|---|
| ① | UI 树定位 | Windows UI Automation：按部件节点身份（Name/AutomationId/ControlType/父路径）在页面树内定位 | ★★★★★ | 不是所有应用都暴露 UIA（Canvas、游戏、部分 Electron/跨平台框架会"空白"） |
| ② | 部件相似度 | 部件形状（多模板 + 多尺度）与文字（OCR）融合打分 | ★★★★ | 纯图标部件只能靠形状；主题/布局大改仍可能失配；相似部件多时需消歧 |
| ③ | 页面内坐标 | 录制时记录的部件相对操作页面左上角的坐标 | ★★★ | 页面内布局/滚动变化即失效；页面没找到时不可用 |
| ④ | AI 语义定位 | 语义理解"这是登录按钮" | ★★★★★（但慢/贵） | 延迟、成本、可能需联网 |

**关键设计**：

1. **双截图录制（页面 + 部件）**：先框"操作页面"（第一次截图），再框页面内的"有效部件"（第二次截图）——页面决定**坐标系与搜索边界**，部件决定**操作对象与点击范围**。两次截图采集的信号（页面上下文 / UI 树 / 形状 / 文字 / 页面内坐标）随 Target 持久化，字段清单见 4.3，用户零感知。

2. **操作页面绑定（先定页面，再定部件）**：解决"多窗口/多页面"歧义的关键。录制时确定性记录页面上下文（进程名 + 窗口标题可通配 + 类名）；运行时先定位页面（跨机运行时 exe 路径会变，故按进程名/标题模糊匹配，而非死磕绝对路径），只在页面范围内搜索部件。同页内多实例部件用 UI 树路径/相对位置消歧；页面定位失败时可"全屏找页面"（OCR/图像）作为最后手段。

3. **部件相似度 = 形状 + 文字融合**：文字信号（OCR）对主题/分辨率变化更鲁棒、默认加权更高；形状信号（多张模板 + 0.8~1.2 倍多尺度 + 投票）兜底；两路信号指向同一位置 → 高置信。`match` 偏好（`text_first` / `image_first`）只调整这一级内部的权重，见第 11 章。

4. **多模板容错**：一个部件可存多张形状模板（不同主题/分辨率下的样子，用户可"再截一张"）；运行时逐张匹配 + 多尺度，任一命中即成功。

5. **页面内坐标（第三级兜底）**：录制时记录部件范围相对页面左上角的坐标；运行时页面定位成功后直接换算——页面整体移动/窗口拖动不影响，页面缩放按页面截图尺寸比例换算。只有页面内布局改变才失效，因此它作兜底而非首选。

6. **置信度融合**：每级策略返回 `{found, location, confidence, method}`，定位器按权重融合（多级指向同一位置 → 置信度叠加），而非简单"第一个命中就用"。

- 默认只用 ①~③（完全离线免费，覆盖绝大多数场景）；①~③ 全失败，或用户打开"AI 增强"，才动用 ④。
- 每级策略带成本/延迟/离线属性，由配置决定启用哪几级。

> 为什么 UI 树值得优先：它是**结构定位**而非像素定位，部件身份由控件树决定，天然抗分辨率/主题/位置变化，且无需截图、速度快，甚至能直接调用控件（InvokePattern）而不必模拟点击坐标。但正因覆盖不全，只能作"辅助"而非"唯一"，与"相似度 + 页面内坐标"两级互补。

### 7.4 AI 的四种用法（按价值排序）

| # | 用法 | 解决什么 | 优先级 |
|---|---|---|---|
| 1 | **视觉定位（grounding）** | 自然语言/截图 → 目标坐标，抗主题/分辨率/布局变化 | 直接提升准确度 |
| 2 | **结果语义判定** | 做完动作后判断"成功 / 报错 / 断网 / 密码错" | 直接支撑第 6 章错误处理 |
| 3 | **脚本生成** | 一句话（"帮我自动登录"）→ 步骤 | M4 落地，降低创建门槛 |
| 4 | **图片脚本 → 代码转译** | 读全部页面/部件截图，导出可读源码模块（双导出出口 ②，见 8.9） | M4+，最终版 |

> **关于"识别截图属于哪个应用、获取路径"**：这件事**不需要专门的 LLM**——截图时就能用 WinAPI（`WindowFromPoint` → `GetWindowThreadProcessId` → `QueryFullProcessImageName`）确定性地拿到进程名、exe 路径、窗口标题，100% 准确、零成本。LLM 的合理角色是**兜底验证**：当操作页面定位失败（脚本在别的机器/不同环境运行，页面上下文匹配不上）时，用多模态模型看屏判断"当前是不是目标应用的页面"，作为上下文校验的最后手段。

### 7.5 模型选项与权衡

| 方案 | 定位准确度 | 延迟 | 成本 | 离线 | 中文 |
|---|---|---|---|---|---|
| OpenCV 模板匹配（基线） | 中，对变化敏感 | <50ms | 免费 | ✅ | — |
| RapidOCR 文本定位 | 高（文字场景） | 100–300ms | 免费 | ✅ | ✅ |
| Grounding DINO（本地视觉定位） | 高 | 100ms–1s | 免费 | ✅ | 中 |
| Qwen-VL（本地多模态小模型） | 高 | 1–3s | 免费 | ✅ | ✅ |
| 云多模态 API（GPT-4o / Qwen-VL-Max / DeepSeek-VL） | 很高 | 1–3s | 按调用计费 | ❌ 需联网 | 看模型 |

**运行方式与提醒**：AI 增强（视觉定位、结果语义判定、脚本生成）**默认走云端 API**——准确度最高、免去本地部署大模型；代价是屏幕截图/文字会上传到第三方服务器，且每次调用有 1~3s 网络延迟，故首次启用需**显式授权 + 知情提示**（说明上传内容与用途）。核心定位链 ①~③（无 AI 参与）仍完全离线可用；需要离线或强隐私时，可在设置中切换为本地模型（表中本地方案）。

### 7.6 结论

1. **能显著提升准确度**：AI 视觉定位把"像素匹配"升级为"语义匹配"，是解决模板匹配脆弱性的根本方案。
2. **但不是银弹**：有延迟、成本、隐私三方面代价，所以做成**按需降级**而非唯一依赖。
3. **一石二鸟**：AI 视觉能力同时服务"定位"（7.4-1）和"错误判定"（7.4-2），是第 6 章错误处理的天然升级路径。

---

## 8. 脚本导出为 Skill 与 Tools

> 本章新增需求：把用户搭好的脚本，导出成 **Agent 可直接调用的 Skill + 配套 Tools**。其本质是一次**"函数拼装 + 添加修饰"**——把引擎里的原子函数按脚本步骤拼成一条调用链，再分别给函数和整条链套上"工具修饰"和"技能修饰"。

### 8.1 背景与本质：函数拼装 + 修饰

前七章的产品是"给人用的"：用户搭脚本 → 脚本引擎跑。本章打通第二条路：**同一个脚本，也能变成"给 Agent 用的"能力**。

核心认知：**每个简单操作背后，引擎里都已经有一个纯函数**（点击 `click`、输入 `type_text`、等待 `wait`、结果校验 `verify_result`…）。因此"导出 tools/skills"不是凭空生成新代码，而是两件事：

1. **拼装函数** —— 脚本的步骤序列 → 一条函数调用链
2. **添加修饰** —— 给原子函数加"工具修饰"（`@tool`），给拼好的链加"技能修饰"（`@skill`）

```python
# 引擎里已有的原子函数（纯函数，无 AI）
def click(target): ...
def type_text(target, text): ...
def verify_result(expected): ...

# ① 导出 Tools = 给原子函数加"工具修饰"
@tool(description="点击屏幕上某个目标")
def click(target): ...

# ② 导出 Skill = 拼装函数调用链 + 加"技能修饰"
@skill(
    name="auto_login",
    description="自动登录：输入账号密码并点击登录，校验是否成功",
    system_prompt="你是自动登录助手，负责帮用户登录系统",
)
def auto_login(username: str, password: str):
    type_text("用户名", username)   # 脚本步骤 1 → 函数调用
    type_text("密码", password)     # 脚本步骤 2 → 函数调用
    click("登录")                   # 脚本步骤 3 → 函数调用
    verify_result("登录成功")       # 脚本步骤 4 → 结果校验（第 6 章）
```

> 本功能承接笔记 2 的架构约定（`Skill 与 Agent 分离`、`工具纯净化`、`registry.py` 函数 → tool schema）。

### 8.2 两类产物

转化产出两类东西：

**① Tools —— 原子操作（全局共享，不随脚本重复）**

执行引擎的底层原语本来就是纯 Python 函数（无 AI），经 `registry.py` 转成 OpenAI 兼容的 tool schema 后，Agent 即可调用：

| Tool | 对应引擎原语 | 给 LLM 的描述 |
|---|---|---|
| `find_target` | 目标识别引擎 | "在指定操作页面内定位部件，返回点击点（三级降级）" |
| `click` | 输入模拟 | "点击某目标 / 某坐标" |
| `type_text` | 输入模拟 | "在目标处输入文字" |
| `wait` | 调度 | "等待 N 秒" |
| `screenshot` | 截图 | "截取当前屏幕（可返回给多模态模型）" |
| `notify` | 提示原语 | "弹出白话提示并暂停，等待用户处理（脚本需要用户介入/确认时调用）" |
| `verify_result` | 结果判定器（第 6 章） | "判断操作结果：成功 / 失败(类型) / 未知" |

> 这些 Tools 是**一份全局实现**，所有脚本导出的 Skill 共用，不需要每个脚本复制一份。

> ⚠️ **导出物不是自包含代码**：skill/tools 只含"调用编排 + 部件数据（WIDGETS）"；`find_target` 等原语的识别/输入实现（mss 截图、RapidOCR、OpenCV 相似度、UIA、SendInput）仍在**本机引擎**上执行——Agent 与本产品同机时，screen.py 以引擎库或本地服务（IPC）方式调用。运行时每次动作前仍要"看屏幕找部件"（① UI 树 → ② 部件相似度 → ③ 页面内坐标，见 7.3），图片识别真实发生，识别失败同样走 L1 兜底（重试 → 提示我）。

**② Skill —— 一个脚本 → 一个 Skill**

对照笔记 2 的 Skill 三要素：

| Skill 要素 | 从脚本生成 |
|---|---|
| `name` | 脚本名（如 `auto_login`） |
| `description` | 从步骤自动总结（"输入账号密码并点击登录，校验是否成功"） |
| `run(params)` / `build_task(params)` | 执行脚本 + 参数填入 |

### 8.3 两种执行模式：拼装时机不同

两种模式的本质区别是：**"拼装函数链"发生在导出时，还是运行时由 LLM 完成**。

| 模式 | 拼装时机 | Skill 形态 | Agent 职责 | 特点 |
|---|---|---|---|---|
| **A 确定性封装** | **导出时拼好** | `@skill` 包住拼好的函数链，`run(params)` 直接跑 | 只决定"调哪个 skill + 传什么参" | 可靠、确定；脚本逻辑不被改写 |
| **B LLM 编排** | **运行时拼** | 只加 `@tool`（原子操作清单）+ `SYSTEM_PROMPT`（脚本意图） | LLM 自己按意图动态调 tool 拼装 | 灵活、能自主容错；引入不确定性 |

> **导出即拼装完成**：无论哪种模式，导出动作在**一次性完成**后产出落盘的 skill 文件与 tools 文件——模式 A 的 skill 是拼好的调用链（快照式，运行时不改）；模式 B 的产物是原子 tools 清单 + SYSTEM_PROMPT（由 Agent 在运行时按意图调用，但清单本身也是导出时静态确定的）。

→ **MVP 推荐模式 A**（确定性优先）；模式 B 作进阶，见 8.5。

### 8.4 参数化：脚本字面量 → Skill 入参

脚本里的写死值（输入文本、等待时长、循环次数）在导出时识别为**可参数化点**，转成 skill 入参：

```
脚本步骤：输入"用户名框"文本 "admin"
        →  skill.run(username="admin", ...)   # "admin" 变成可改参数
```

这样 Agent 能说"帮我用 zhangsan/123456 登录"，而不必重新搭脚本。

### 8.5 与结果判定器的结合（关键价值）

`verify_result` 作为 tool 暴露后，LLM Agent 能像人一样"边做边看结果"：

```
LLM 调用 click("登录") → 调用 verify_result()
  → 返回 { status: "failure", failure_type: "auth" }
  → LLM 自主决策：换密码重试 / 停下问用户 / 放弃
```

这正好把第 6 章"结果级失败的自主恢复"从"用户预设分支"升级为"LLM 运行时决策"。**结果判定器的输出，正是 LLM 编排模式最需要的信息**。

### 8.6 目标 Agent 目录适配与自动写入

导出不是"生成文件让用户自己拷贝"，而是：**用户给出其 Agent 的安装/项目目录 → 系统自动检测该 Agent 的工具与格式约定 → 拼装出适配它的 skills 与 tools → 自动写入对应文件夹**。

1. **目录接入与自动检测（只读）**：导出向导中输入或选择 Agent 目录（可保存多个配置）。系统扫描目录以识别框架与约定：目录结构（`skills/`、`tools/`、`plugins/` 等）、工具定义方式（装饰器 / JSON Schema / manifest 等）、加载入口（`registry.py`、`package.json`、Agent 配置文件等），并列出该 Agent **已注册的工具清单**，作为拼装依据。

2. **按 Agent 拼装（仍发生在导出时）**：拼装时机与 8.3 模式 A 一致，只是格式按目标 Agent 适配：
   - **原子 tools**：与检测到的已有工具做**差集**——Agent 已有同名/同能力工具则复用（不重复写、不覆盖），缺失的才生成（如 `find_target`、`type_text`、`verify_result`）；
   - **skill**：按该 Agent 的 skill 约定生成（三要素 + SYSTEM_PROMPT + TOOLS 静态清单，见 8.7），引用的工具名与它已注册的名字对齐。

3. **自动写入**：默认先**干跑预览**（列出将新建/覆盖的文件与差异，用户确认后再落盘）；写入前做冲突检测（同名 skill/tool → "覆盖 / 跳过 / 改名"）；写入目标 Agent 的 `skills/` 与 `tools/` 对应文件夹；完成后提示"刷新 / 重启 Agent 以加载"。

> 边界：检测与写入都限定在用户指定的目录内；识别不出框架时退回**通用目录导出**（见 8.7）并提示用户手动放置。

### 8.7 导出产物文件结构

```
导出到 Agent 项目（默认通用格式，复用笔记 2 的目录约定；已识别目标 Agent 时，
按其 8.6 检测结果落到对应的 skills/、tools/ 文件夹）:
├── skills/
│   └── auto_login.py         # ← 从"自动登录"脚本生成（导出时拼装完成）
│       ├── name / description
│       ├── SYSTEM_PROMPT     # 脚本意图 + 使用说明
│       ├── TOOLS = [find_target, click, type_text, verify_result]   # 导出时静态确定
│       └── build_task(params) # 参数组装 + 脚本步骤翻译
└── tools/
    ├── screen.py             # find_target / click / type_text / verify_result
    │                         # + WIDGETS 注册表：本脚本用到的部件数据（见下）
    └── registry.py           # Python 函数 → LLM tool schema（复用）
```

**部件数据存放：内嵌 WIDGETS 注册表**（定案）。脚本引用的部件信号（页面上下文 / UI 树节点 / 文字 / base64 图像 / 页面内坐标 / match）在导出时**内嵌序列化进 `tools/screen.py` 的 `WIDGETS` 注册表**——skill 按部件名引用数据、各 Agent 目录单文件携带，接受体积代价：

```python
# tools/screen.py（片段）
WIDGETS = {
    "登录按钮": {                                  # 与脚本 target 同名引用
        "page":   {"process": "chrome", "title": "*登录*", "class": "..."},
        "uia":    {"type": "Button", "name": "登录", "parent_path": "..."},   # ①
        "text":   "登录",
        "images": ["data:image/png;base64,...."],                              # ②（可多张）
        "coord":  {"x": 420, "y": 560, "w": 120, "h": 40},                    # ③
        "match":  "text_first",
    },
    # ……其余部件（用户名框、密码框…）同构追加
}
```

> 与 11 章脚本存储同规则（16.1 #9：MVP 内嵌）；多个脚本共用同一部件时，screen.py 注册表按全局去重合并（同名部件不重复内嵌）。若未来单文件体积过大，再参照 #9 的外链思路另行定案。

### 8.8 转化流程（拼装 + 适配 + 写入视角）

```
脚本 (JSON)
  → 解析步骤 → 得到"原子操作 + 参数"序列
  → 拼装：每个步骤 → 一次函数调用，按顺序串成调用链（导出时完成）
  → 参数化：把写死的字面量提升为函数入参
  → 数据：步骤引用的部件信号（页面/UI 树/文字/图像/坐标）内嵌序列化为
           tools/screen.py 的 WIDGETS 注册表（base64 图像，全局去重合并）
  → 修饰①：给用到的原子函数加 @tool（生成 tool schema）
  → 修饰②：给整条调用链加 @skill（name/description/SYSTEM_PROMPT）
  → 适配：扫描用户指定的 Agent 目录 → 按检测到的格式与已有工具调整产物
  → 写入：干跑预览 + 冲突检测 → 自动写入 Agent 的 skills/、tools/ 文件夹
```

### 8.9 双导出出口：图片脚本 / 代码脚本（最终版，用户自选）

同一脚本在最终版本具备**两条导出出口**，导出向导中由用户自主选择（可同时导出）：

| 出口 | 产物 | 执行方式 | 适合 |
|---|---|---|---|
| ① 图片脚本导出 | Skill + Tools（部件数据内嵌 WIDGETS，见 8.7） | Agent 调用本机引擎做识别/输入（见 8.2 ⚠️ 注） | 所有用户；与录制行为保真、可靠 |
| ② 代码脚本导出（AI 转译） | 云端多模态读全部页面/部件截图 → 可读 Python/TS 源码模块 | 源码可在 Agent 环境直接运行（UIA/坐标代码），或作为嵌入参考 | 开发者；可读、可维护、可脱离本产品 |

**规则与边界**：

- **触发**：导出向导选择"图片脚本 / 代码脚本 / 两者都要"；② 走云端 API（16.1 #2），上传截图前需显式授权。
- **转译输入输出**：输入 = 步骤序列 + Target 全量（页面图 / 部件图 / OCR 文本 / UIA 信息）；输出 = 带注释的每步源码 + 参数接口。与 7.4 #3"一句话 → 步骤"共用同一条 AI 转译管线。
- **保真与安全**：② 不替代 ①——WIDGETS 图片信号始终保留、可随时回退；AI 引用不存在的部件时，导出前做引用完整性校验拦截；产物标注"由 vX 脚本生成，修改脚本后请重新导出"（单向导出，不反向同步）。
- **能力边界**：仅当目标应用暴露可编程元素（UIA）时，② 才能产出"无图直接运行"的代码；Canvas/游戏/自绘 UI 等无元素可译场景退化为坐标/视觉语义代码，或提示用户改用出口 ①。
- **落地与写入**：② 同样支持 8.6 的目标目录适配与自动写入（落点由用户选择：Agent 的 scripts/ 目录等，或手动放置）。

### 8.10 范围说明（新增受众）

本功能引入了**第二类用户**：Agent / 智能体开发者。它不改变"面向所有人"的核心定位（搭建仍然四操作、零代码），只是给脚本增加了**导出通道（双出口，见 8.9）**：既能在本产品里跑，也能导出给 Agent 用。建议作为独立模块（含目录检测与写入与 AI 转译），不影响主链路。

---

## 9. 技术架构

### 9.1 总体分层

```
┌─────────────────────────────────────────────┐
│              用户界面层 (UI)                  │
│   脚本编辑器 / 录制器 / 截图选区 / 运行面板      │
└──────────────────────┬──────────────────────┘
                       │ 读写 Script (JSON)
┌──────────────────────▼──────────────────────┐
│             脚本模型层 (Model)                │
│   Script / Step / Target / Condition / Loop   │
│        ← 序列化 / 反序列化 (JSON) →            │
└──────────────────────┬──────────────────────┘
                       │ 解释执行
┌──────────────────────▼──────────────────────┐
│             执行引擎层 (Engine)               │
│   解释器(顺序/分支/循环) · 调度 · 容错/重试    │
│              · 结果校验 (第6章)                │
└──────────┬────────────────────┬──────────────┘
           │                    │
┌──────────▼──────────┐  ┌──────▼──────────────┐
│   目标识别引擎 (Vision) │  │   输入模拟层 (Input)  │
│ 双截图 · OCR · 相似度   │  │  鼠标/键盘 · 坐标换算  │
│  · AI视觉定位(可插拔)   │  │  (Windows SendInput)  │
└─────────────────────┘  └─────────────────────┘
```

> **架构形态（16.1 #6 定案后）**：UI 为 Electron 桌面壳（节点图画布等），引擎为独立 Python 进程（截图 / OCR / 识别 / 输入 / 执行 / 判定），两者经本地 IPC（如 JSON-RPC）通信——UI 与引擎解耦，引擎可脱离 UI 独立测试与复用。**同一引擎进程也服务第 8 章导出的 Agent tools**：Agent 运行 skill 时经本地 IPC 调用识别/输入原语（find_target / click / …），图片识别仍在引擎上执行（见 8.2 注）。

### 9.2 各层职责（关键点）

- UI 层：全屏半透明选区层是技术难点。
- 模型层：脚本即 JSON，可存文件、可分享。
- 执行引擎：纯逻辑，不依赖具体 UI。
- 目标识别层：产品的技术核心，最易出错。
- 输入模拟层：需处理 DPI 缩放。

> 各层具体职责见 9.1 图。

> 第 8 章的"脚本导出 Skill/Tools"是架构的**旁路出口**：它读取"模型层"的脚本，调用"执行引擎"的原语（目标识别 + 输入模拟 + 结果判定），但输出到 Agent 世界（`skills/` + `tools/`），不影响主运行链路。

### 9.3 关键流程：一次"运行"

```
用户点击"运行"
  → 引擎加载 Script
  → 逐步骤执行：
      ├─ 动作步骤：定位操作页面 → 页内三级定位部件 → 模拟输入 → (可选)校验预期结果
      ├─ 条件步骤：页内定位部件(是否出现) → 进入 then / else 分支
      └─ 循环步骤：进入 body，按次数/条件重复
  → 每一步都写日志（用户视角："第2步：找到'登录按钮'，点击成功"）
  → 结果校验失败 / 异常 / 用户手动停止 → 按第6章策略处理
```

---

## 10. 关键技术选型

### 10.1 语言：引擎 Python，界面层 Web（Electron）

依据：屏幕识别 / OCR / 输入模拟生态在 Python 最成熟（既有项目与学习笔记均为 Python：`desktop_agent.py`、`main.py`）；富交互 UI（节点图编辑器）采用 **Electron + Web 技术**（16.1 #6）。两者解耦：Python 引擎进程负责截图 / 识别 / 执行 / 判定，Electron 负责界面，经本地 IPC 协作。

### 10.2 核心依赖候选（Windows 专属）

| 能力 | 候选库 | 推荐 | 理由 |
|---|---|---|---|
| 屏幕截图 | `mss` / `PIL.ImageGrab` | **mss** | 快（毫秒级全屏）、Windows 稳定 |
| 图像匹配 | OpenCV `matchTemplate` | **opencv-python** | 成熟、归一化相关匹配鲁棒 |
| OCR（中文优先） | `RapidOCR` / `PaddleOCR` | **RapidOCR** | 已安装(3.8.1)、ONNX 轻量、离线、中文强；免 paddlepaddle 依赖 |
| UIA 元素定位 | `uiautomation` / `pywinauto` / `comtypes` | **uiautomation** | Windows UIA 封装：hit-test、取 Name/AutomationId、InvokePattern |
| 鼠标键盘 | `pynput` / `pyautogui` / `pywin32` | **pywin32 + pynput** | Windows 原生 `SendInput` 更可靠 |
| DPI 适配 | `ctypes` 调 WinAPI | `SetProcessDpiAwarenessContext` | 精确控制高 DPI 缩放 |
| 桌面壳 | PySide6 / Tkinter / **Electron** | **Electron** | 16.1 #6（应用形态）：Web UI 生态 + 现成节点图库；与 Python 引擎进程经本地 IPC |
| 节点图编辑器 | LogicFlow / LiteGraph / Rete（Electron 内嵌） | **LogicFlow**（落地前与 LiteGraph / Rete 各做 demo 后定） | 中文文档活跃、支持自定义节点（条件/循环端口）；Canvas 渲染性能充足 |
| AI 视觉定位（可选） | Grounding DINO / Qwen-VL / 云 API | 见 7.5，**默认不启用** | 按需增强 |
| 脚本序列化 | 标准库 `json` | **json** | 脚本即 `.json` 文件，可分享 |
| Agent 互操作 | OpenAI SDK / registry.py | 复用笔记 2 的 `registry.py` | 工具函数 → LLM tool schema |

> Windows 专属红利：可以调用 Windows 原生能力（`SendInput` 事件级模拟、`SetProcessDpiAwareness` 解决缩放、UIA 文本接口做定位增强），比跨平台方案更可靠，且不必为 macOS/Linux 兼容买单。

### 10.3 三个技术难点提前预警

1. **全屏截图选区层**：需要一个无边框、全屏、半透明的顶层窗口，用户在其中拖拽画矩形。Electron 用透明无边框 BrowserWindow（`transparent` + `alwaysOnTop` + 局部 `setIgnoreMouseEvents` 穿透）承载框选交互；屏幕图像与坐标换算由 Python 引擎（mss）提供并经 IPC 回传。注意多显示器与 DPI 缩放下的坐标对齐。
2. **DPI 缩放适配**：Windows 高 DPI（125%/150% 缩放）下，屏幕物理像素 ≠ 逻辑坐标，截图像素与鼠标坐标会错位。需在启动时声明 DPI 感知（`SetProcessDpiAwarenessContext`），并统一坐标系。
3. **部件形状匹配的缩放不变性**：用户在不同分辨率电脑上运行脚本时，录制时的部件形状模板尺寸会变。对策：多尺度匹配（模板按 0.8~1.2 倍缩放逐次匹配），②级内文字优先策略可大部分规避，AI 视觉定位可彻底规避。

### 10.4 选型原则：复用优先，自研延后

> 技术策略：**底层能力一律复用成熟 Python 包，不提前自研；自研只留给"产品逻辑本身"，规模化自研等到项目真正落地之后**。

- **复用清单**：10.2 表全表——截图 mss、图像匹配 OpenCV、OCR RapidOCR、UIA uiautomation、输入模拟 pywin32/pynput、桌面壳 Electron、节点图画布 LogicFlow 等现成 Web 图库、DPI 用系统 API、序列化 json、Agent 互操作复用笔记 2 的 registry.py、AI 默认接云端现成 API（本地模型为可选，见 7.5）。
- **属于产品逻辑、必须自研的部分**（无现成包可用，非"重造轮子"）：双截图选区交互、脚本模型与解释器、结果判定器（6.4）、导出器（第 8 章）——这正是产品差异所在。
- **自研触发条件（落地后）**：产品跑通真实用户场景后，用数据证明瓶颈（某级定位命中率不足、OCR 延迟超标、交互不顺），再针对性地自研/替换该环节；落地前不投入。
- 对应路线：M0~M4 全部建立在现有包之上；潜在自研项（自研节点图、专用识别模型等）只作为落地后的待评估 backlog，不阻塞主路线。

---

## 11. 数据模型（脚本格式）

脚本以 **JSON 文件** 存储（`.sgscript.json`），既是运行格式，也是分享/版本控制格式。设计草案（含错误处理与预期结果字段；下文示例重在展示字段结构与嵌套方式，业务片段之间的拼接仅为示意，连贯示例见 4.1）：

```json
{
  "version": "1.0",
  "name": "自动登录",
  "steps": [
    {
      "id": "s1",
      "type": "action",
      "action": "type",
      "target": { "text": "用户名", "match": "text_first" },
      "params": { "text": "admin" }
    },
    {
      "id": "s2",
      "type": "action",
      "action": "type",
      "target": { "text": "密码", "match": "text_first" },
      "params": { "text": "••••••" }
    },
    {
      "id": "s3",
      "type": "action",
      "action": "click",
      "target": { "image": "data:image/png;base64,....", "text": "登录", "match": "text_first" },
      "params": {},
      "expected_outcome": {
        "target": { "text": "登录成功", "match": "text_first" },
        "on_fail": { "strategy": "notify", "message": "用户名或密码可能不对" }
      }
    },
    {
      "id": "s4",
      "type": "condition",
      "condition": { "target": { "text": "网络连接失败", "match": "text_first" }, "exists": true },
      "then": [
        { "id": "s4a", "type": "action", "action": "notify", "params": { "message": "请检查网络后点继续" } }
      ],
      "else": []
    },
    {
      "id": "s5",
      "type": "loop",
      "loop": { "mode": "count", "count": 10 },
      "body": [
        { "id": "s5a", "type": "action", "action": "click", "target": { "text": "下一页" } }
      ]
    }
  ]
}
```

**设计要点**：

- `target.image` 存**部件范围截图**（第二次截图，base64 内嵌），脚本可单文件分享（MVP）；若截图过大，可改为外链图片文件 + 相对路径（V2）。
- `target.text`（部件 OCR 文字）、`target.match`、`target.semantic`（语义描述，M3 起由 AI 视觉定位启用）由系统自动填充，用户无感知；第一次截图的页面级信息存于 `target.page`（页面上下文 + 页面截图/尺寸）。
- `target.match` 只微调**部件相似度（②级）内部**的文字/形状权重：`auto`（默认，**文字优先**、形状兜底的融合打分，信号缺失时自动退化为单路）/ `text_first`（强制文字信号最高权重，适合文字稳定部件）/ `image_first`（纯图标或文字不可靠部件，形状为主）；是否启用 AI 语义定位（④）由"高级设置"开关控制，默认关闭。
- Target 是**完整对象**：`page`（操作页面：上下文 + 页面截图 + 尺寸）、UI 树节点、多张形状模板、页面内坐标等内部字段（见 4.3）在序列化时**全部持久化**进 JSON（上例仅摘录展示），保证脚本跨机/隔次运行时三级定位有足够信号（见 7.3）；多张图像内嵌会增大文件，V2 改外链（16.1 #9）。
- `params` 里的字面量可在编辑时标记为"可改写"，导出 Skill 时转成入参（见 8.4）；JSON 预留标记写法 `"params": { "text": { "$param": "username", "$default": "admin" } }`，MVP 阶段由导出器启发式识别文本字面量，不要求用户手动标记。
- `expected_outcome` 承载"做完后应该看到什么" + 失败策略（`on_fail`），是第 6 章 L2 的落点。
- `on_fail.strategy` 取值：`retry`（重试）/ `notify`（提示我）/ `stop`（停止）/ `skip`（跳过）。
- 三种步骤通过 `type` 字段区分，字段结构统一，便于序列化与校验。
- 所有 `id` 用于 UI 定位、日志引用、循环跳转。

> **导出相关性**：第 8 章的"脚本 → Skill"转化器直接读取这份 JSON——`steps` 决定 tool 清单与 `SYSTEM_PROMPT`，`params` 里的字面量被识别为可参数化点，`expected_outcome` 映射为 `verify_result` 调用。双导出出口中的 AI 代码转译（8.9 ②）同样以本 JSON 及 Target 全量为输入。

---

## 12. UI/UX 设计

### 12.1 界面布局：节点图工作区

脚本编辑器采用**节点图（方块 + 有向连线）**表示操作链：每个操作是一个"节点"，节点之间用带箭头的线连接表示执行顺序；画布支持**缩放/平移**，长脚本可放大看细节、缩小看全貌。技术实现：Electron + Web 节点图库（16.1 #6）；MVP 可先用简单列表式编辑器验证核心链路（见 15 章 M1）。

```
┌───────────────────────────────────────────────────┐
│  脚本名：[自动登录]  [运行 ▶][保存][导出Skill][⤢整理]│
├──────────────┬────────────────────────────────────┤
│ 节点面板      │  节点图画布（滚轮缩放 / 拖拽平移）     │
│ ┌─────────┐  │                                      │
│ │ [点击]   │  │      [开始]                           │
│ │ [输入]   │  │        │                              │
│ │ [等待]   │  │        ▼                              │
│ │ [快捷键]  │  │  [点击 登录 🖼]                       │
│ │ [如果]   │  │        │                              │
│ │ [重复]   │  │        ▼                              │
│ └─────────┘  │  [输入 用户名 "admin"]                 │
│              │        │                              │
│              │        ▼                              │
│              │    ┌──[如果看到 验证码]──┐             │
│              │    │是                    │否          │
│              │    ▼                      │           │
│              │ [等待 60 秒]              │           │
│              │    └──────────┬───────────┘           │
│              │               ▼                       │
│              │   [重复 10 次]─┐                       │
│              │       │循环体   │                       │
│              │       ▼        │                       │
│              │  [点击 下一页]  │                       │
│              │       └────┬───┘                       │
│              │            ▼                          │
│              │          [结束]                       │
├──────────────┴────────────────────────────────────┤
│  运行日志：第2步 ✓ 点击成功                          │
└───────────────────────────────────────────────────┘
```

> 上图为示意：节点面板是**可滚动列表**，动作节点按 5.2 的 MVP 动作集完整提供（含 [双击]、[提示我] 等，上图仅节选显示，未穷举）；结构节点种类见下表。

**节点类型（对应三种步骤 + 动作）**：

| 节点 | 形状 | 端口 | 对应数据 |
|---|---|---|---|
| 动作节点 | 矩形 | 1 输入 + 1 输出 | action 步骤 |
| 条件节点 | 菱形 | 1 输入 + 2 输出（是/否） | condition 步骤的 then/else |
| 循环节点 | 带框矩形 | 1 输入 + 2 输出（循环体/完成） | loop 步骤的 body/后续 |
| 开始/结束节点 | 圆角 | 仅输出 / 仅输入 | 脚本起止（可选） |

> 动作节点可**附带"校验"标记**（做完后应该看到 X + 失败时策略），对应第 6 章 L2 的预期结果校验。

**连线（有向边）**：

- 从节点输出端口拖到下一节点输入端口，箭头表示执行顺序。
- 条件节点的两个输出对应"是/否"；循环节点的两个输出对应"循环体/完成后"。

**画布交互（关键需求）**：

- **缩放**：滚轮缩放，放大看细节、缩小看整体流程。
- **平移**：拖拽空白处或按住空格平移画布。
- **自动布局**：一键整理为整齐的纵向流，避免连线交叉。

**参数编辑（"只改要改的节点"）**：

- 双击节点在侧边面板改参数（目标截图/动作/文本），改某节点不影响其他节点。
- 点节点上的目标缩略图可重新截图。

### 12.2 两种创建方式

| 方式 | 说明 | 适用 |
|---|---|---|
| **拖节点连线** | 从节点面板拖节点到画布，用连线连接 | 精确控制 / 改脚本 |
| **录制模式（强烈推荐，M3 落地）** | 用户正常操作电脑，系统**自动把整个过程拆成一块块"积木"**（每次点击/输入/滚动 → 自动推断动作 + 捕获页面与部件），边录边生成节点图脚本 | 零门槛、最契合"面向所有人" |

> **录制拆解机制**：录制中系统监控鼠标点击 / 键盘输入 / 滚轮 / 快捷键——对每个操作实时推断动作类型（"点一下"/输入文字/滚动…），自动沿用当前"操作页面"并捕获操作点部件（双截图），随即生成一块"积木"；操作间的无感停顿自动插入"等待"积木。录制结束即得完整节点图脚本，用户可在画布上改名、删除误录步骤、补"做完后应该看到"校验。**用户视角的"积木" = 系统内部的步骤节点**（与 17 章术语表"搭积木"心智一致）。
>
> **录制模式**是"面向所有人"的杀手级功能，但实现复杂（需实时截屏差分检测鼠标点击位置、自动推断动作类型）。建议 MVP 先做手动搭建，验证核心链路后于 M3 上录制。

### 12.3 交互细节（体现"不暴露 RPA"）

- 所有按钮/文案只用白话：`点一下`、`输入文字`、`如果看到`、`重复几次`、`做完后应该看到`。
- 报错也是人话：目标找不到时报 `没找到"登录"，请确认屏幕上有这个按钮`；结果校验失败时报 `做完后没看到"登录成功"，可能是用户名或密码不对`。
- 运行日志逐条显示、带 ✓/✗ 图标、可点击定位到对应步骤。

---

## 13. 非功能性需求

| 维度 | 要求 | 说明 |
|---|---|---|
| **可靠性** | 目标未找到自动重试（默认 3 次，间隔 1s），超时后报错并暂停 | 降低脆弱性 |
| **结果可验证** | 动作支持"预期结果"校验，失败走 6.3 策略 | 支撑错误处理 |
| **可中断** | 全局停止热键 + UI 停止按钮，任意时刻可中断 | 循环/无限循环安全 |
| **性能** | 单步部件定位 < 500ms（UI 树/相似度离线快路径；OCR 可异步缓存） | 保证脚本运行流畅 |
| **跨分辨率** | UI 树 → 部件相似度（文字 + 多尺度形状）→ 页面内坐标 三级降级（+ AI 可选） | 换电脑/改缩放/整页移动仍能跑 |
| **离线优先** | 核心定位链（UI 树/相似度/页面坐标）完全离线可用；AI 增强默认云端、可切本地 | 核心功能不依赖网络 |
| **安全** | 不注入进程、不读内存；云端 AI 需用户显式授权并知情上传 | 明确能力边界 |
| **可移植** | 脚本为纯 JSON，可导出/导入/分享 | 一人录制，多人复用 |
| **可互操作** | 脚本可导出为 Agent 的 Skill + Tools（第 8 章） | 打通 Agent 世界 |

---

## 14. 风险与挑战

| 风险 | 等级 | 说明 | 缓解措施 |
|---|---|---|---|
| 目标匹配脆弱 | 🔴 高 | 界面改版、分辨率变化、动态内容都会让截图失效 | UI 树 → 部件相似度 → 页面内坐标 三级降级 + 重试 + AI 增强（见 7.3） |
| **结果级失败未被察觉** | 🔴 高 | 密码错/断网时工具不知道结果不对 | 预期结果校验（第 6 章 L2）+ AI 语义判定（M3） |
| 多目标歧义 | 🟡 中 | 多个窗口/多个相同按钮（跨窗口 + 窗口内多实例） | 操作页面绑定（先定页面再定部件）+ UI 树路径/索引/相对位置消歧 |
| 动态数字/倒计时 | 🟡 中 | 含数字的按钮截图无法匹配 | OCR 时对数字区域做容错，或引导框选不含数字的部分 |
| AI 增强的隐私/成本 | 🟡 中 | 云端 AI 上传屏幕内容、有延迟与费用 | 默认云端：首次启用显式授权 + 知情提示；隐私敏感场景可切本地模型 |
| 无限循环失控 | 🟡 中 | 条件循环条件永不满足 / 无限循环无人停止 | 条件循环默认次数上限（最多 1000 次，可调）；无限循环靠强制全局停止热键 + UI 停止按钮兜底（见 5.4） |
| DPI 坐标错位 | 🟢 低 | 高 DPI 屏截图与点击坐标错位 | 启动时声明 DPI 感知，统一坐标换算 |
| 范围膨胀 | 🟡 中 | "顺便加个变量吧"导致退回 RPA | 严守"四操作"边界，新增功能走"高级设置"渐进披露 |
| Skill 导出的 LLM 编排不确定性 | 🟡 中 | 模式 B 把流程交给 LLM，可能偏离脚本意图 | 默认模式 A（确定性）；模式 B 用 SYSTEM_PROMPT 强约束 + 白名单工具集 |

---

## 15. 里程碑与路线图

> 注：里程碑 M0~M4 是开发阶段编号；文中保留的"V2/V3"指产品发布版本（V1 = M1/M2 达成后的首个发布版），两者不混用。

### M0 — 技术验证（Spike，可丢弃）
- 跑通最小闭环：`双截图（先框操作页面，再框部件）→ 页内按 UI 树/相似度/坐标三级找部件 → 模拟点击`。
- 验证核心件：mss 截图、uiautomation（UI 树命中与定位）、OpenCV 模板匹配、RapidOCR 文本定位、pywin32 点击，在目标机器上 DPI 缩放正常。
- 产出：一个命令行验证脚本（非产品代码）。
- **✅ 已完成（2026-09-09，详情与原始数据见《M0_评审摘要.md》《M0_进展记录.md》，位于 docs/）**：
  - 常规场景（网页登录/ERP/动态页，各 ≥100 轮，含移窗）**98.7% HIT**（dev ≤1px）；
  - 无 UIA 自绘窗口（Tk，40 轮）**97.5%**；真实 Win32 客户端（控制面板，40 轮）**92.5%**（不移窗 100%）；
    真实客户端静态子页（bilibili 设置页，20 轮）**100%**；
  - **强动态桌面客户端（bilibili 视频流 feed，30 轮）仅 3%**——整窗模板对持续变化内容失效
    → M2 需要局部特征/语义定位；产品侧引导用户停在静态页是可行兜底（实证 100%）；
  - **UWP 窗口在本机（远程/虚拟化会话特征）呈“渲染幽灵”**（置顶 API 成功但屏幕不渲染，
    calc/set 双目标实证）→ UWP 类数据需物理机补测，属环境限制非算法问题；
  - **OCR 实测**：RapidOCR rec ~130–160ms/行（本机），整页 OCR 10s+ → M1 部件定位必须
    小区域条带 + 页面文本索引缓存；**推理后端改用官方 onnxruntime(CPU)**（比 onnxruntime-directml
    轮子的 CPU 执行快 ~2×；directml 包保留为 GPU 机选项）；
  - ④ Electron↔Python IPC 原型：稳态往返 452ms（<1s 达标）、坐标 1px 无错位 @125% DPI
    （150% 复测在 M1 联调补做）；UIA 覆盖率首份数据与 Edge DOM UIA 坐标偏差（~140px，
    链内已消歧）均记录在案。
  - **M1 范围收敛清单（启动文档 §8 判定为 GO 的输入）**：首版 = 网页 + 标准控件 + 静态自绘
    桌面应用；强动态桌面客户端与 UWP 图标类列入 M2；③云端 AI 结论见下（已完成，2026-09-09，
    智谱 glm-4.6v）：
    **AI 视觉能力实测**：看图读部件文字 5/5（文字型 100%，含图标/数字键整体 71%）；界面文案
    语义判定 10/12（83%，凭据/网络类 100%）；**像素坐标直出 0/7、3x3 区域 1/5、长列表文本
    候选选择 4/7 —— VLM 直接定位/选目标准确率不足** → M3 架构定论：**坐标定位维持本地三级
    （OCR/模板/UIA，M0① 已证 98.7%），云端 AI 仅承担 D3 语义判定与语义确认**；延迟实测：
    视觉请求 3.5–51s（推理模型，大图更慢）、文本 1.2–6.5s → 语义判定按异步/非实时路径设计；
    若未来确需纯视觉定位，再评估原生 grounding 服务或自训检测器（受 10.4 复用红线约束）。
    **VLM 像素定位深测（v0.29 追加）**：提示格式决定性——归一化坐标输出（glm-4.6v）7 目标
    框内 5/7、dev 中位 10px，像素坐标/思考链/bbox 均 ≤2/7；glm-4v-flash/plus 定位 0/7；
    重复采样 10/20，login 恒 6px、深色/同名歧义/小图标类漂移 100–200px，误差非线性不可校准 →
    **纯 VLM 像素定位不可作独立定位源**；定位层级如 M3 引入 AI：归一化粗定位圈区域（±100px）
    + 本地精定位融合，且仅异步路径（详见 docs/M0_进展记录.md ③-VLM 深测）。

### M1 — MVP（手动搭建版）
- Electron + Python 引擎（本地 IPC）单窗口 GUI：编辑器（MVP 先用列表式验证核心链路，节点图基于 Web 图库）+ 运行日志。
- 支持动作步骤（单击/双击/输入/等待/提示我/快捷键）+ 双截图选区（页面 + 部件）。
- 支持条件（如果看到）+ 循环（重复 N 次 / 直到看到）。
- 内置 L1 兜底（重试 + 提示），**预期结果校验（L2）**。
- 脚本保存/加载为 JSON。
- 验收标准：**一个零编程用户能在 5 分钟内搭出"自动登录"脚本（含失败提示）并成功运行。**

### M2 — 可靠性增强
- 部件相似度融合定位（文字 + 形状权重调优）、UI 树路径消歧、页面内坐标降级完善。
- 显式条件错误处理（L3）完善、循环次数上限、更好的错误提示与日志。
- 脚本导入/导出分享。

### M3 — AI 增强 + 录制模式
- 可插拔 AI 视觉定位（**默认云端 API**；本地模型作为隐私/离线选项）。
- AI 结果语义判定（区分"断网/密码错/成功"）。
- 操作录制：自动生成"双截图（页面 → 部件）→ 动作"脚本。

### M4 — 脚本导出 Skill/Tools + 自然语言生成
- **脚本 → Skill + Tools 导出器（第 8 章）**：导出时拼装（模式 A 先行）+ 目标 Agent 目录自动检测与写入（8.6）。
- "帮我做一个自动登录" → 自动生成步骤。
- 双导出出口：**图片脚本导出（①）与 AI 代码脚本转译（②，读截图生成可读源码）**，导出向导由用户自选（8.9）。
- 脚本编辑：拖拽排序、复制粘贴步骤。

---

## 16. 开放问题与待决策项

> 已确认的决策与仍需拍板的事项分开列出。

### 16.1 已确认

| # | 决策 | 结论 |
|---|---|---|
| 1 | 目标平台 | ✅ **仅 Windows**（已确认） |
| 2 | AI 运行方式 | ✅ **默认云端 API**（准确度优先、免本地大模型）；本地模型为隐私/离线选项（见 7.5） |
| 3 | OCR 方式 | ✅ **本地 RapidOCR**（已安装，离线、中文强，见 10.2） |
| 4 | 录制模式优先级 | ✅ **M3**（先验证手动搭建闭环，见 15 章） |
| 5 | 脚本导出 Skill 的执行模式 | ✅ **确定性封装（A）为主**；B（LLM 编排）作进阶（第 8 章） |
| 6 | 应用形态 / GUI 技术栈 | ✅ **Electron + Web 节点图库**（LogicFlow 推荐，落地前 demo 选定）；Python 引擎为独立进程，经本地 IPC 协作 |
| 7 | AI 增强的引入节奏 | ✅ **M3 才上**（MVP 用离线三级链已够用） |
| 8 | 预期结果校验（L2） | ✅ **MVP 就做**（是错误处理的根基） |
| 9 | 目标存储 | ✅ **MVP 内嵌、V2 外链**（见 11 章） |

### 16.2 待确认

> 当前无待确认项——16.1 #1~#9 已全部定案；后续出现新的开放问题在此登记。

---

## 17. 附录：术语对照表

> 这是"不暴露 RPA"的落地核心：**左列是用户看到的，右列是系统内部真实含义**。产品任何界面、文案、报错只允许出现左列词汇。

| 用户看到的（对外） | 系统内部（技术） |
|---|---|
| 截图目标：先框页面，再框部件 | 双截图：操作页面（窗口上下文 + 坐标系）+ 有效部件（形状 / 文字 / UI 树 / 页面内坐标） |
| 积木（录制产物） | 一个步骤节点（动作/条件/循环）：录制时按一次操作自动拆分生成，用户可删改整理 |
| 操作页面 | 第一次截图确定的窗口/页面：脚本的坐标系与搜索范围 |
| 有效部件范围 / 点击范围 | 第二次截图框选的部件区域；点击默认落在范围中心 |
| 点一下 / 点两下 / 点右键 | 鼠标单击 / 双击 / 右键事件 |
| 输入文字 | 键盘文本输入模拟 |
| 等一下 | 固定时长等待（sleep） |
| 按快捷键 | 组合键事件模拟 |
| 如果看到 X | 条件分支：目标 X 存在性判定 |
| 如果没看到 X | 条件分支：目标 X 不存在判定 |
| 重复 N 次 | 固定次数循环（for） |
| 一直重复，直到看到 X | 条件循环（while） |
| 一直重复 | 无限循环 + 手动停止 |
| **做完后应该看到 X** | **预期结果校验（结果级验证）** |
| **再试一次** | **自动重试机制** |
| **提示我** | **暂停 + 人工介入** |
| **检测成功/失败** | **结果判定器：D1 环境探测 + D2 OCR 特征库 + D3 AI** |
| **它认出来了 / AI 帮我找** | **AI 视觉定位（语义匹配）** |
| **导出给智能体用** | **检测 Agent 安装目录 → 按其约定拼装 Skill/Tools → 自动写入对应文件夹（Agent 互操作）** |
| **导出成代码脚本** | **AI 读全部截图 → 转译成可读源码模块（双导出出口之二，见 8.9）** |
| 没找到"登录" | 部件三级定位全部失败（UI 树 / 相似度 / 页面内坐标） |
| 做完后没看到"登录成功" | 结果校验失败（预期结果未出现） |
| 运行 / 停止 | 执行引擎启动 / 中断 |
| 脚本 | 有序步骤列表（JSON 序列化） |

---

## 附：文档变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-09-06 | 初稿：定位、用户、四操作、架构、选型、数据模型、风险、路线图 |
| v0.2 | 2026-09-06 | 新增第 6 章「错误处理与结果校验」、第 7 章「AI 增强」；确认仅 Windows；更新架构/选型/数据模型/风险/路线图/开放问题 |
| v0.3 | 2026-09-06 | 新增 6.5「结果级失败的判定方案（核心算法）」；OCR 选型由 PaddleOCR 改为 RapidOCR（已安装） |
| v0.4 | 2026-09-06 | 新增第 8 章「脚本导出为 Skill 与 Tools」；更新架构/选型/风险/里程碑/开放问题/术语表；新增受众说明 |
| v0.5 | 2026-09-06 | 7.3 升级为「多策略定位器 + 多模板容错」：新增 UIA 元素定位、坐标定位；目标对象支持多模板与多路信号自动采集 |
| v0.6 | 2026-09-06 | 新增「窗口上下文绑定（先定窗口再定元素）」解决多窗口歧义；明确"应用路径识别用确定性 WinAPI、LLM 仅作兜底验证" |
| v0.7 | 2026-09-07 | UI 从"步骤列表+编辑区"改为「积木式工作区」：动作/条件/循环积木、凹槽拼接、参数内联编辑；更新技术选型与决策 A |
| v0.8 | 2026-09-07 | 修正：编辑器由"积木式"改为「节点图」——方块 + 有向连线表示操作链，画布支持缩放/平移/自动布局；更新 3.3、12 章、技术选型与决策 A |
| v0.9 | 2026-09-07 | 评审修订：日期对齐；16.1 待确认项归位；结果判定层级改 **D1/D2/D3**（防与 6.2 的 L1~L3 混淆，`layer` 值同步）；5.1/11 章降级链表述统一并定义 `match` 枚举；JSON 示例补密码步骤；动作清单补"提示我"；Target 持久化与参数化点说明；循环兜底精确化；6.3/4.1 措辞修正 |
| v0.10 | 2026-09-07 | 一致性补漏：M1 动作列表、8.2 Tools、12.1 面板图注补"提示我"；清除 12.2 遗留"积木串"旧词 |
| v0.11 | 2026-09-07 | 目标录制改**双截图**（页面 + 部件）与三级定位 **① UI 树 → ② 部件相似度 → ③ 页面内坐标**（④ AI 可选）；同步 4.3/5.1/7.3/9/11/12/13/14/15/17 章 |
| v0.12 | 2026-09-07 | 明确降级规则：UI 树为空直接跳过本级；相似度识别不出才用页面内坐标；`match` auto = 文字优先；修正"无目标"动作表述 |
| v0.13 | 2026-09-07 | 全文精简：删除重复说明（4.3 尾注、判定器首段重复句、7.3 采集罗列、8.1/8.3 赘述、9.2 职责表压缩为要点、头部维护注）；原"AI 增强"小节并入 D3 小节（现 6.4.4）；判定方案章节 6.5→6.4（子节 6.4.1~6.4.6）；修正悬空引用"问题 1" |
| v0.14 | 2026-09-07 | 第 8 章升级（按用户评审意见）：① 明确 **导出即拼装完成**——产物为导出时静态确定的快照（8.3 注）；② 新增 8.6「目标 Agent 目录适配与自动写入」——用户提供 Agent 安装目录 → 只读检测其工具与格式约定（已有工具求差集、不覆盖）→ 按其约定拼装 skill/tools → 干跑预览 + 冲突检测后**自动写入**其 skills/、tools/ 文件夹；③ 原 8.6~8.8 顺延为 8.7~8.9，文件结构与转化流程加入适配/写入步骤；④ 术语表"导出给智能体用"、M4 里程碑同步 |
| v0.15 | 2026-09-07 | 新增选型原则（按用户评审意见）：**复用优先，自研延后**——底层能力一律用成熟 Python 包；自研只留产品逻辑本身（选区交互/解释器/判定器/导出器），规模化自研等落地后用数据证明瓶颈再投入。落到 3.2 设计原则新行、10.4 新小节；10.2 节点图编辑器行注明"决策 A 待定但按复用优先不倾向 QGraphicsView 自研"；M0~M4 全部基于现有包 |
| v0.16 | 2026-09-07 | 错误修正（全文复查）：① 版本标注清理——"V2/V3"未定义且与里程碑冲突，统一为里程碑（AI 语义判定/D3/录制 = M3、脚本生成 = M4、semantic = M3 启用），M3/M4 标题去掉版本号，15 章加"M0~M4 与 V2/V3 不混用"注；② M1"步骤列表"旧表述与 12.1 节点图定稿矛盾，改为"列表式先行、节点图随决策 A"，12.1 同步加注；③ 术语表"检测成功/失败"改用 D1/D2/D3；④ 3.2/6.2 残留单截图措辞改为双截图；⑤ 8.2 `find_target` 描述更新为页面内三级定位；⑥ 10.3 难点改"部件形状匹配"；⑦ v0.11/v0.13 变更行自修正（补 12 章、时序矛盾） |
| v0.17 | 2026-09-07 | 决策 C 定案（按用户评审意见）：**AI 默认走云端 API**。① 决策 C 移入 16.1 #2（已确认），16.2 删除 C 行；② 7.5 段落改为"AI 增强默认云端、首次启用显式授权 + 知情提示、核心链 ①~③ 仍离线、本地模型可切"；③ 13 章"离线优先"、14 章 AI 风险缓解、M3 视觉定位 bullet、10.4 复用清单同步反转；④ 16.1 注释去"原决策 #2"歧义 |
| v0.18 | 2026-09-07 | 16.2 表修订：① 移除 v0.17 遗留的字母断档（C 缺号 + 解释注），D~H **顺移补位为 C~G**，正文"决策 F"引用同步改"决策 E"（11 章）；② B 行理由同步新定位模型（"离线三级链已够用"，替换过时的"OCR+模板匹配"）；③ 待确认项缩至 A~G |
| v0.19 | 2026-09-07 | 决策批量确认（按用户拍板）：**OCR = 本地 RapidOCR、录制 = M3、导出执行模式 = 确定性封装 A 为主**移入 16.1 #3~#5（AI 默认云端已于 v0.17 确认 #2）；16.2 删除对应行并顺移补位为 A~D（预期校验=C、目标存储=D），11 章"决策 E"引用同步改"决策 D" |
| v0.20 | 2026-09-07 | 决策 A 定案（按用户拍板）：**应用形态 = Electron + Web 节点图库**（LogicFlow 推荐，落地前 demo 选定）。① 16.1 新增 #6，16.2 删 A 行，B/C/D 顺移为 A/B/C，11 章"决策 D"引用同步改"决策 C"；② 架构形态确定：Electron UI ↔ Python 引擎进程（本地 IPC），9.1 图后新增注；③ 10.1 语言章节改"引擎 Python + 界面层 Web"；10.2 桌面壳/节点图编辑器两行改 Electron/LogicFlow；10.3 选区层改 Electron 透明窗 + Python mss 实现；10.4 复用清单更新；④ 12.1 括注、M1 里程碑同步（Electron + IPC）；⑤ 全文"决策 A"引用改写为 16.1 #6（避免与新 16.2 A 混淆） |
| v0.21 | 2026-09-07 | 16.2 剩余三项全部按建议确认（按用户拍板）：**AI 增强 = M3 才上、预期结果校验 L2 = MVP 就做、目标存储 = MVP 内嵌 V2 外链**，移入 16.1 #7~#9；16.2 改为"当前无待确认项"（预留未来登记位）；11 章"决策 C"引用改写为"16.1 #9"——至此 16 章决策全部定案（16.1 #1~#9），正文无字母式决策引用（仅 changelog 历史行保留） |
| v0.22 | 2026-09-07 | 导出物部件数据存放定案（按用户拍板）：**内嵌 `tools/screen.py` 的 WIDGETS 注册表**（base64 图像，单文件携带，接受体积代价）。8.7 文件结构更新并新增"部件数据存放"小节（含注册表示例代码与去重合并规则）；8.8 转化流程补"数据内嵌"一步；与 11 章存储规则（16.1 #9 MVP 内嵌）保持一致 |
| v0.23 | 2026-09-07 | 明确导出物运行时依赖（按用户确认）：**Agent 运行时仍要使用图片识别**——导出的 skill/tools 非自包含代码，`find_target` 等原语识别/输入实现在本机引擎执行（mss/OCR/模板/UIA/SendInput），每次动作前仍走三级降级定位。8.2 Tools 注新增 ⚠️ 说明；9 章架构形态注补充"引擎进程同时服务导出的 Agent tools（本地 IPC）" |
| v0.24 | 2026-09-07 | 新增**双导出出口**（按用户拍板，最终版功能）：同一脚本可导出 ① 图片脚本（Skill + WIDGETS，现有第 8 章）或 ② 代码脚本（AI 读全部截图转译可读源码，云端 API），导出向导用户自选。新增 8.9 小节（原范围说明顺延 8.10）含规则与边界（单向导出/引用校验/无 UIA 时退化/同样支持目录适配写入）；7.4 AI 用法表加 #4；M4 里程碑与术语表同步 |
| v0.25 | 2026-09-07 | 录制模式机制细化（按用户确认）：录制时系统**自动把整个过程拆成一块块"积木"**——监控点击/输入/滚轮/快捷键 → 实时推断动作 + 双截图捕获 → 每操作生成一个步骤节点，停顿自动插"等待"，录制结束得完整节点图脚本供用户整理。12.2 表与注更新，明确"用户视角的积木 = 系统内部步骤节点"；术语表新增"积木"词条 |
| v0.26 | 2026-09-07 | 终检修正：① 7.4 标题"三种用法"→"四种用法"（v0.24 加 #4 后行数与标题不符）；② 11 章"导出相关性"注补充"AI 代码转译（8.9 ②）同样以 JSON + Target 为输入"；③ 全面复核确认：章节/小节编号连续（含 6.4.1~6.4.6、8.1~8.10），全部交叉引用有效，代码围栏闭合，术语与决策状态一致 |
| v0.27 | 2026-09-09 | M0 Spike 完成登记（§15 M0 节补完成结论与数据）：常规场景 98.7%/无 UIA 自绘 97.5%/真实客户端 92.5~100%；强动态客户端 3%（feed）→ M2 局部定位 + 引导静态页兜底；UWP“渲染幽灵”环境限制定论；OCR rec 延迟实测与官方 onnxruntime(CPU) 后端选择（决策 #3 的实现注）；④ IPC 452ms/1px@125% 达标；M1 范围收敛清单成型。原始数据/详录在 docs/M0_评审摘要.md、docs/M0_进展记录.md |
| v0.28 | 2026-09-09 | M0③ 云端 AI 实测完成登记（智谱 glm-4.6v，用户提供账号）：看图读字 100%（文字型）、语义判定 83%、像素坐标 0/7、区域 1/5、长列表候选选择 4/7、延迟视觉 3.5–51s/文本 1.2–6.5s → M3 架构定论：坐标走本地三级定位，云端 AI 承担 D3 语义判定（§15 M0 节更新；决策 #2 云端 API 不变） |
| v0.29 | 2026-09-09 | VLM 像素定位深测登记（§15 M0 ③ 追加）：提示格式决定性（归一化坐标 71% 框内 vs 像素 29%/思考 0%/bbox 0%）；glm-4.6v 为智谱系唯一可用、flash/plus 0/7；重复采样 login 恒 6px、深色/歧义/小图标漂移 100–200px、误差不可校准 → 纯 VLM 像素定位不可作独立定位源，如引入 AI 定位采用“归一化粗定位圈区 + 本地精定位”且仅异步 |
