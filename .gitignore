# Python
__pycache__/
*.pyc
.venv/
venv/
.venv*/

# 临时冒烟产物（M0 前留下的临时文件）
.smoke_m0.py
.smoke_ocr2.py
_tpl.png

# IDE / 工具
.idea/
.claude/

# 运行时杂项
*.log
*.err
smoke/diag/_msg.txt
capture_tmp/

# 只对本机有意义的临时脚本（里面写死了本机绝对路径：桌面位置、用户名等）。
# 仓库要能被别人 clone 后直接用，这类文件不入库。
smoke/diag/_launch_*.txt
smoke/diag/_make_*.py
smoke/diag/_make_*.ps1
smoke/diag/_split_*.py
smoke/diag/_fix_*.py
smoke/diag/_publish_audit.py

# 本地密钥（云端 AI 的 API Key 等）：**绝不允许进仓库**。
# 为什么要这条：Windows 的 setx 只对"之后新启动的进程"生效，而引擎是被已经运行着的宿主
# （Electron/DSH）拉起来的、继承的是旧环境 —— setx 形同没设，所以 Key 需要本地文件兜底加载。
.secrets/
smoke/.secrets/
*.env

# 跑批产物（M2-1）：原始逐轮数据、失败截图与汇总报告都不入仓库（体量大、可重跑）
engine/scripts/bench_raw.jsonl
engine/scripts/bench_evidence.jsonl
engine/scripts/bench_shots/
engine/scripts/bench_report.md
# 跨分辨率归档（M2-WP8）：原始数据不入仓库
engine/scripts/bench_archives/
# M0 跑批聚合（smoke/m0_bench.py 生成）
smoke/data/m0_bench_summary.md

smoke/electron_proto/node_modules/

# Electron UI（app/）：依赖与打包产物不入库（package-lock.json 保留以复现安装）
app/node_modules/
app/dist/
app/out/


# 录制回放测试的逐场景结果（派生产物，脚本可重跑）
smoke/rec/replay_s*.json
smoke/diag/replay_stacks.txt

# 运行时定位日志（引擎默认落盘位置，属产物不入库）
engine_loc.jsonl
*_loc.jsonl

# 开发内部文档（设计文档、里程碑计划、验收数据、收口记录）：不随仓库发布 ——
# 别人 clone 下来照 README 就能装起来用，不需要这些过程资料。
# 文件仍保留在本地，需要查阅时见 git 提交历史。
/docs/
/项目分析文档.md
/项目启动文档.md
/项目开发过程文档.md
