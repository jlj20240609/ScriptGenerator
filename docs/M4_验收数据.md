# M4 验收数据 — 脚本导出 Skill/Tools + 自然语言生成

生成时间：2026-09-11　依据：过程文档 §3 M4 门禁、分析文档 §8 / §8.9 / §7.4

## DoD 三项

### ① 导出产物在真实 Agent 目录加载运行通过

复现：`smoke/.venv-ortcpu/Scripts/python.exe smoke/diag/export_e2e.py`

| 检查 | 结果 |
| --- | --- |
| 导出到真实目录结构（skills/ + tools/） | ✓ 产出 `skills/自动登录.py`、`tools/screen.py`（含 WIDGETS）、`tools/registry.py` |
| 从该目录 **import** 产物 | ✓ |
| `build_task(用户名="zhangsan", 密码="s3cret")` | ✓ 参数真的填进了脚本，且产物里不再有写死的旧账号 |
| 把产物产出的脚本交给引擎**真跑** | ✓ 3 步全 ok、界面真的转场到首页、**无人工介入** |
| 目标目录的**只读扫描** | ✓ 认出已有工具 `click / find_target / schemas` |
| 差集复用（已有工具不重写、不覆盖） | ✓ 产物里是转发壳 `_agent_tool("click")` |
| **目标 Agent 自己的实现原样未动** | ✓ 逐字节相同 |
| 复用的工具真的转发到对方实现 | ✓ `bind_agent_tools` 后调用返回对方的实现 |
| 干跑预览阶段不落盘 | ✓ 查文件系统确认 |
| 写入后提示刷新/重启 | ✓ |

**如实说明**：验收用的"目标 Agent 目录"是我按 §8.6 的约定**合成**的（含 `skills/`、`tools/`、
自定义 `registry.py` 与已有 `click/find_target` 实现）。用户手头若有真实 Agent 项目目录，
把这个脚本指向它即可复跑——脚本只读扫描 + 写入指定目录，不需要任何本产品内部约定。

### ② AI 代码脚本转译产物通过引用完整性校验

复现：`smoke/.venv-ortcpu/Scripts/python.exe smoke/diag/ai_export_eval.py`（真云端 glm-4.6v）

| 检查 | 结果 |
| --- | --- |
| 读页面/部件截图转译成可读源码 | ✓ 9.5s、**1924 tokens**（输入 1333 / 输出 591）、读了 5 张图 |
| 产物形状 | ✓ `run(username, password)`：写死的值提升成了**函数入参**，每步带中文注释 |
| **引用完整性** | ✓ 产物引用的部件（用户名/密码/登录/登录成功）全部存在于脚本 |
| 编造部件会被拦下 | ✓ 单测：AI 写 `click("登录按纽")` → **不给出产物**并报出问题（12 条测试之一） |
| 没授权时干净降级 | ✓ 对照跑 `--no-ai`：一次都不上传，并明确指向出口 ① |
| 单向导出标注正确（DoD ③） | ✓ 头部含来源脚本、targets_rev、"修改脚本后请重新导出" |
| 坐标不出自这份代码 | ✓ 头部写明"坐标仍由本机引擎在运行时求" |

**产物样本**：`smoke/rec/ai_translated.py`

### ③ 单向导出标注正确

产物头部固定包含：来源脚本名 + `targets_rev` + 转译/导出时间 + 模型名 +
"**修改脚本后请重新导出**：这份源码不会反向同步回脚本"，以及"导出物不是自包含代码"的
边界说明。出口 ① 与出口 ② 的产物都有；界面自测里也直接读盘核对了这条标注。

## 双导出出口与一句话生成

| 项 | 证据 |
| --- | --- |
| 出口自选（§8.9 图片脚本 / 代码脚本 / 两个都要） | 界面自测：向导第一步三个选项 ✓ |
| 出口 ② 与出口 ① 共用同一条写入通道 | 干跑预览 + 冲突策略 + 备份 + 刷新提示 ✓ |
| **一句话 → 步骤骨架** | 真云端 3 次：1246 / 1523 tokens；都产出 2 步（type「用户名」+ click「登录」） |
| 骨架里**没有任何坐标** | ✓ 每次都用 JSON 全量扫描 `rect/center/coord/image` |
| 模型自己标注不确定处 | ✓ 例："用户没有提供密码，所以没有输入密码" |
| **本地按文字补齐坐标** | ✓ 在真实窗口上找到 2 个部件，坐标与真值对比 **2/2 命中** |
| 补齐后的脚本被引擎跑通（真机一次） | ✓ 2026-09-11：按钮回调被触发 1 次（那次运行的日志在提交信息里） |
| 补齐不猜：找不到的如实列出 | ✓ `missing` 字段 + 界面提示"还差 N 个" |

## 云端用量（成本口径，用户 2026-09-11 定的口径）

| 场景 | 调用 | tokens |
| --- | --- | --- |
| AI 代码转译（出口 ②，1 个脚本） | 1 次 | 1924 |
| 一句话生成（1 句话） | 1 次 | 1246 / 1523（两次运行） |
| 图片脚本导出（出口 ①） | **0 次** | 0（纯本地拼装，不调云端） |
| 本地按文字补齐坐标 | **0 次** | 0（本机 OCR，云端不参与） |

## 边界自检（每条都有测试钉住，不是靠自觉）

- 生成/转译结果里**不许出现坐标**：出现就整份丢弃（测试：`test_no_coordinates_anywhere`）
- 模型自己塞坐标也带不出去（测试：`test_ai_supplied_coordinates_are_rejected`）
- 坐标只来自本地：补齐用的是**用户框的那张页面截图**本机 OCR 的结果
- 覆盖别人的文件必须先明确选择；默认跳过（测试 + 界面自测都查了文件系统）

## 测试与自测总数

| 套件 | 条数 |
| --- | --- |
| 引擎测试（exporter / agent_dir / ai_export / ai_generate / ipc_export / ipc_generate） | **82 条全过** |
| 界面自测 `--autotest-ui` | **70 条全过**（录制相关默认跳过，见收口记录） |

**已知局限**（如实登记，详见收口记录）：

1. 一句话生成里，若 AI 给的文字在页面上是**标签**（如"用户名"），文本匹配会补到标签上
   而不是输入框——纯文本匹配的固有歧义，需要用户把这一步的目标改一下。
2. `smoke/diag/generate_e2e.py` 只验到"生成 + 补齐 + 真值对比"；真跑那一步依赖实时桌面
   的页面定位（受窗口层级/遮挡影响），已通过一次但不作为日常复跑项。
3. 出口 ② 的产物质量依赖云端模型，本表数字是能力证明，不是稳定性承诺。
