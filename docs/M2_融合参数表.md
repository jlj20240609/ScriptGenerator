# M2 融合参数表（WP3：相似度融合权重调优）

- 生成时间：2026-09-11 00:39:36
- 证据：392 条（正例 356 / 负例 36），分组 6 个
- 划分：训练 332 条 / 留出 60 条（**按场景分组切**，留出场景：dyn, login）
- 口径：命中 = 选中真值候选；误报 = 没命中却选了别的（代价 4×，宁缺勿滥）；目标值 = 命中率 − 4 × 误报率

## 1. 结论

- 训练集最优：`allow_far=True, max_dist=240, prefer_nearby=True, text_sim_min=0.7` → 命中 0.870，误报 0.130
- **留出集复验**：线上默认参数 命中 1.000 / 误报 0.000；训练最优参数 命中 1.000 / 误报 0.000
- 换参数后**行为变化**的样本：0 条（难度标签：—）；留出集含的标签：live
- 建议：**训练集最优与线上默认在本批证据上等价**（留出集目标值差 +0.000）——没有给出更好的取值，维持现状。要判断还能不能更好，得先把证据覆盖面加宽（见第 4 节）。

## 2. 每个参数的取值依据（单参数敏感性：固定其他为最优值，在**全部证据**上扫）

| 参数 | 取值 | 命中率 | 误报率 | 目标值 |
|---|---|---|---|---|
| text_sim_min | 0.7 | 0.890 | 0.110 | 0.452 |
| text_sim_min ←当前 | 0.75 | 0.890 | 0.110 | 0.452 |
| text_sim_min | 0.8 | 0.890 | 0.110 | 0.452 |
| text_sim_min | 0.85 | 0.890 | 0.110 | 0.452 |
| prefer_nearby ←当前 | True | 0.890 | 0.110 | 0.452 |
| prefer_nearby | False | 0.870 | 0.130 | 0.349 |
| allow_far ←当前 | True | 0.890 | 0.110 | 0.452 |
| allow_far | False | 0.890 | 0.110 | 0.452 |
| max_dist | 240 | 0.890 | 0.110 | 0.452 |
| max_dist | 360 | 0.890 | 0.110 | 0.452 |
| max_dist ←当前 | 600 | 0.890 | 0.110 | 0.452 |
| max_dist | 1000 | 0.890 | 0.110 | 0.452 |

## 3. 网格搜索前 12（训练集）

| # | 参数 | 命中率 | 误报率 | 目标值 |
|---|---|---|---|---|
| 1 | allow_far=True, max_dist=240, prefer_nearby=True, text_sim_min=0.7 | 0.870 | 0.130 | 0.352 |
| 2 | allow_far=True, max_dist=360, prefer_nearby=True, text_sim_min=0.7 | 0.870 | 0.130 | 0.352 |
| 3 | allow_far=True, max_dist=600, prefer_nearby=True, text_sim_min=0.7 | 0.870 | 0.130 | 0.352 |
| 4 | allow_far=True, max_dist=1000, prefer_nearby=True, text_sim_min=0.7 | 0.870 | 0.130 | 0.352 |
| 5 | allow_far=False, max_dist=240, prefer_nearby=True, text_sim_min=0.7 | 0.870 | 0.130 | 0.352 |
| 6 | allow_far=False, max_dist=360, prefer_nearby=True, text_sim_min=0.7 | 0.870 | 0.130 | 0.352 |
| 7 | allow_far=False, max_dist=600, prefer_nearby=True, text_sim_min=0.7 | 0.870 | 0.130 | 0.352 |
| 8 | allow_far=False, max_dist=1000, prefer_nearby=True, text_sim_min=0.7 | 0.870 | 0.130 | 0.352 |
| 9 | allow_far=True, max_dist=240, prefer_nearby=True, text_sim_min=0.75 | 0.870 | 0.130 | 0.352 |
| 10 | allow_far=True, max_dist=360, prefer_nearby=True, text_sim_min=0.75 | 0.870 | 0.130 | 0.352 |
| 11 | allow_far=True, max_dist=600, prefer_nearby=True, text_sim_min=0.75 | 0.870 | 0.130 | 0.352 |
| 12 | allow_far=True, max_dist=1000, prefer_nearby=True, text_sim_min=0.75 | 0.870 | 0.130 | 0.352 |

## 4. 证据覆盖面自评（哪些参数**还没被考到**）

- 有区分力的：
  - `prefer_nearby`：0 条「同页同名 + 运行期行序调换」证据（真值只有靠邻居文字才认得出）；
  - `text_sim_min`：0 条字形相近的负例（相似度恰好压线，门槛放宽就会误点）；候选分数分布里中间段（0.5~0.95）有 0 个。
- **还没被考到的**（不要根据本表改这些）：
  - `allow_far` / `far_limit_scale` / `max_dist`：这批证据的候选**全部**落在近处，考不出来；
  - 负例里没有「存在误匹配候选」的样本，误报率的分母偏乐观；
  - 图片/模板路径的参数（`tpl_score_min` / `ring_score_min`）：本轮只采了**文字**候选的证据；
  - 页面级同源三档（`page_sim_min` / `page_sim_soft` / `page_soft_tpl_min`）：属于整窗路径，不在②a 证据里；
- **真正能区分「邻居优先 vs 就近优先」的样本：15 条**（判据：真值候选邻居对得上，且存在离录点更近的干扰候选）。数量少时参数差异会被噪声淹没，结论只能当参考——所以本表里这类参数的建议一律标成「维持现状/待补证据」。
- 结论：本表只足以给 `prefer_nearby` 与 `text_sim_min` 提供依据，其余参数等真机证据（跑批 `--dump-evidence`）再定。
- 场景分组：dyn, erp, feed, interference, login, login_full；近处候选 492 / 远处候选 0（含候选的负例 0 条）。

## 5. 复验注意

- 本表来自**合成证据**（候选层面的差异），真机差异（遮挡/DPI/动态内容/多窗口）没有覆盖 → 必须用 M2-1 跑批证据复验后才写回引擎默认值。
- 留出集按场景切分；若将来把真机证据合并进来，请保持「按脚本/场景分组」的划分方式。
- 证据可由跑批产出（`--dump-evidence`）：真机证据与合成证据同一格式，`--evidence` 直接喂给本脚本。
