# M1 进展记录（引擎波次1：离线纵向切片 E1–E4 + E6 + E8）

> 日期：2026-09-09（v0.31 规格同日）｜波次范围按用户拍板：
> **波次1 = engine/ 包落地（schema/matcher/locator/capture/executor/logger/CLI）+ 合成屏自动化测试**；
> E5 校验模式 + E7 AI 适配层 + 真实窗口回归迁移 = 波次2。
> 规格权威：《M1_引擎设计清单》v0.1、《项目分析文档》v0.31 §5.1/§6.2–6.3/§7.3/§11。

## 1. 波次1交付清单（对照清单 §2 模块表）

| 模块 | 交付 | 状态 | 说明 |
|---|---|---|---|
| `engine/schema.py` | E8 `.sgscript.json` v1.0 读写/校验 | ✅ | `validate/check/load/dump` + 构造助手；Target 全量字段 + anchors + targets_rev；支持 §11 手工简写（无 page 沿用） |
| `engine/matcher.py` | E3 匹配原语 | ✅ | 多尺度模板 0.8~1.25×（大模板金字塔）、像素同源、text_similar 方向规则、OCR 条带、data-url 编解码 |
| `engine/locator.py` | E2 页面+部件定位 | ✅ | `locate_page`（整窗模板 → 静态锚还原页面原点）+ `locate_widget`（页内三级；exists 语义排除③；搜索/点击不越页面范围） |
| `engine/capture.py` | E1 采集 | ✅(离线部分) | PageSpec/锚 spec 构造、跨帧静态复验、锚几何还原；win32/mss 惰性导入（真窗采集实跑留波次2） |
| `engine/executor.py` | E4 解释器 | ✅ | 六动作 × 条件 × 循环 + L1（重试→白话提示）+ L2 预期结果 + on_fail 四策略 + 点击安全闸 + 校验钩子（E5 波次2接入）+ LiveDriver |
| `engine/logger.py` | E6 定位 JSONL | ✅ | `log_loc` / `tail(step_id, n)`，内存版供测试 |
| `engine/cli.py` | 引擎 CLI | ✅ | `validate` / `run`（真实桌面）/ `selftest`；`python -m engine` |
| `engine/tests/` | 自动化测试 | ✅ | 合成屏离线全绿（数字见 §5） |
| `engine/errors.py` | 结构化错误码 | ✅ | 对应开发过程文档 §7（`schema_invalid/page_not_found/widget_not_found/click_guard_failed/loop_limit/verify_failed/stop_requested/...`） |

## 2. 环境实测发现（决定 OCR 参数，回写 matcher 文档注）

- **问题**：rapidocr 3.9.2（本机 venv）默认 Det `limit_type=min` + 层级
  `limit_side_len`(736/960/1500/2000)：300×90 小条带会被放大到 **736×2464**，
  单次 OCR 稳态 0.9–6.8s；1280² 以上 det 输入出现 ~15× 非线性的耗时跳变。
- **修复**：引擎 OCR 引擎初始化强制 `Det.limit_type=max, limit_side_len=960`
  （模块级单例）。实测（i9-13900H，物理中文渲染 + 真实 ERP 资产）：
  - 合成 300×90 找“库存查询”：**稳态 ~90–140ms/次**（识别正确）
  - 真实 ERP 页面菜单条带 260×70：**~81ms/次**（识别“库存查询”正确）
  - 整页 1288×988 全页 OCR 仍 ~10s → 引擎默认**不做整页 OCR**，
    部件/存在性搜索一律“小区域条带”（见 §4 设计注）
- 模板匹配（cv2 5.0，CPU）：300×90 模板全屏搜索稳态 **36ms**；合成页 1.15× 缩放偏移定位 **145ms**。
- M0 结论复用：OCR rec 慢 → “条带优先 + 命中即停”策略落地 locator（框相关行内收紧带，
  替代固定 (300,100)/(650,260) 高条带——实测高条带把页面其他行/框线的 det 盒成批卷进，
  600×194 单次可达 4–6s）。
- **性能风险登记（2026-09-09 实测并定案/登记）**：
  1. OCR 漂移（已定案修复）：默认(-1)/1 线程在 ~15 次连续推理后漂移到 0.4~4.9s/次；
     **intra_op_num_threads=4 + inter=1 连续 20+ 次稳定 37–51ms/条带** → 引擎默认采用；
  2. ORT 推理后紧邻的 cv2 匹配偶发短暂变慢（数百 ms，数拍恢复）→ 引擎固定
     cv2 单线程 + 大模板“金字塔粗选 top-2 全分辨率精修”（页面定位实测 ~0.6–1.1s）；
     部件模板按已锁定页面缩放**单档匹配**（文字仍是主信号）；
  3. 验收口径：单步部件定位 ≤500ms 以模板路径中位数为准；OCR 条带路径按实测波动登记，
     “文本索引缓存”列为 M1 中段优化项。

## 3. 引擎设计定稿点（波次2 及联调引用的契约）

1. **页面定位次序**（清单 §3）：整窗模板 0.8~1.25× → 静态锚；同屏连续步骤用
   “上一矩形快速复验”（reused 命中 ≈ 省一次全屏多尺度扫描），失配自动回落全量搜索。
2. **页内约束**：所有部件搜索在页面裁剪内进行；任何越界候选盒直接丢弃并记
   `*_out_of_page`（构造“页面外诱饵”负例测试通过）。
3. **exists 语义（条件/循环/预期结果共用）**：①UI 树(无提供者跳过) + ②相似度，
   **③页内坐标不参与“看到”**（坐标还在 ≠ 内容还在）——波次1 定稿并测试锁定。
4. **until 循环跨页语义**：条件目标所属页面尚未出现（如登录后跳转中）→ 视为
   “还没看到”继续循环（与 §4.1 示例“直到看到 首页 {等待2s}”一致）；
   达上限（默认 1000，可配）→ `loop_limit` 失败记录，不终止脚本。
5. **点击安全闸引擎化**（M0 实证：校验不过不点）：点击前取点击点周边
   ±210×120 新帧做文字/模板复验；不过闸 = 过程性错误（不发送点击）。
6. **L1/L2 边界**：过程性错误（page/widget/guard）→ 校验钩子（若配）→ L1 自动重试
   N 次 → 白话提示（继续/跳过/停止）；结果性失败（预期结果未出现）→ `on_fail`
   四策略（retry 耗尽后转白话提示），**绝不触发校验入口**（清单 §3 状态机）。
7. **校验钩子契约（E5 波次2 实现）**：`calibrator(CalibRequest) -> CalibResult`；
   `CalibRequest{reason(first_run|page_not_found|widget_not_found|click_guard_failed),
   step, target, page_spec, page_rect, loc_rows, ctx}`；
   `CalibResult{ok, updated, page_spec?, note?}`；`updated+page_spec` → 写回并
   立即重试该步（不消耗 L1 重试）；首次执行校验由 `RunConfig.calibrate_first_run` 开启。
   引擎侧已完成触发/写回/报告（`report.calib`），测试覆盖（mock 校验器）。
8. **HumanIO 契约（Electron IPC 落点）**：`notify(message)`（提示我步骤）、
   `prompt_not_found`（L1，继续/跳过/停止）、`prompt_outcome_fail`（L2 失败，
   继续/停止）；无交互界面时默认“停止”（无人值守安全）。
9. 定位日志行 = `{ts, step_id, event(locate_page|locate_widget|exists_check|outcome_check|
   click_guard|procedural_fail), method(page_tpl|anchor|ocr_text|tpl|page_coord|ai|none|
   ocr_patch|tpl_fallback|guard_off|no_patch), confidence, rect, screen_meta, detail}`；
   点击安全闸每次判定都记日志（含通过）；`tail(step_id,n)` 供校验调阅。
10. **页面命中同源确认（波次1 实测定案）**：整窗模板 CCOEFF 命中后须通过像素同源复验
    （`pixel_sim(thr=16, 240×150) ≥0.82`）才算页面确认——大面积白底/低纹理 UI 会让
    跨页模板假阳性（实测 0.72+ 阈值下异页命中率可达 100%）；改版页同源不过 → 自然走
    锚定位/校验入口，定位日志 detail.page_tpl.sim 记录分值。
11. **C 层挂死护栏（2026-09-09 实测：ORT det 与 cv2 matchTemplate 在本机存在概率性
    冻结 >400s，1.25/1.29 ORT、4.13/5.0 cv2 均复现，numpy 正常——疑似底层并行运行时
    与大小核调度病态）**：OCR 与模板匹配都在守护线程执行（超时 8s/10s）；连续超时
    3 次 → 熔断冷却 30s（期间该路径快速失败，自动退化为下一级定位信号），冷却结束
    OCR 自动重建引擎自愈（`ocr_reset()/cv_reset()` 可手动重置）；测试运行器每个测试
    前重置计数，防跨测试污染。

## 4. 波次1 显式不做的部分（登记，避免范围蔓延）

- UI 树（①级）真实提供者 → 波次2 接 live 窗口（locator 已留 `uia_provider` 槽）；
- 校验模式 E5 / AI 适配层 E7（含授权知情提示）→ 波次2；
- 真实窗口回归资产迁移（fixtures html + targets/*.png → `.sgscript.json`）→ 波次2；
- 全页 OCR 与“文本索引缓存” → 维持关闭/后续（M1 中段按 ≤500ms 实测再定）；
- IPC 契约（JSON-RPC 方法清单/UI 弹窗）→ UI 里程碑联调时立项；
- 手工简写脚本（target 无 page）仅 validate 级通过；运行期页面解析要求至少一步带 page
  （与录制产物一致）；条件页缺失走“否则”并如实记录。

## 5. 自动化测试与验收映射

运行（全部离线合成屏，无需真实桌面/AI key）：
- `python -m engine.tests.run_all`：unittest 标准输出（单进程）；
- `python engine/tests/run_watchdog.py`：串行逐测试实时进度（推荐，隔离熔断计数）；
- `python engine/tests/run_fork.py [可选测试名]`：每测试独立子进程（彻底隔离全局状态）。

**最终结果（2026-09-09，干净环境）：79/79 全绿**（schema 13 + capture 10 + cli 3 +
matcher 8 + locator 8 + executor 37）。

| 测试文件 | 覆盖 | 结果 |
|---|---|---|
| `test_schema.py` | §11/清单§4 结构、重复 id、动作/参数/策略校验、示例 JSON | ✅ 全过 |
| `test_matcher.py` | data-url、多尺度 0.9/1.2×、搜索区域、负例、text_similar 方向、OCR 条带+真实资产、耗时 | ✅ 全过 |
| `test_capture.py` | 锚几何还原（含缩放）、裁剪钳制、跨帧静态复验、PageSpec 构造 | ✅ 全过 |
| `test_locator.py` | 页面模板/缩放/复用、锚恢复（模板失配）、页内三级、exists 排除③、**页面外诱饵负例** | ✅ 全过 |
| `test_executor.py` | 六动作链、条件/循环、L1 提示三分支、校验钩子恢复、首次校验、on_fail 四策略、安全闸、until 上限/跨页、forever 停止、步间停止、日志契约 | ✅ 全过 |

- 清单 §5 验收映射：① 六动作×条件×循环端到端（合成屏）✅；② 页面定位/锚（离线部分）✅，
  真窗 dev≤20px 回归=波次2；③ 页面内约束（诱饵负例）✅；④ 校验模式=波次2；
  ⑤ 首次执行校验=引擎侧就绪（mock 验证）✅/真 AI=波次2；⑥ 定位日志 JSONL + tail ✅，
  结果性失败不触发校验 ✅；⑦ 单步预算：模板路径 <500ms ✅（实测见 §2），OCR 条带路径
  90–140ms 达标（max-960 参数），整页 OCR 显式不做；⑧ `.sgscript.json` 校验 ✅；
  术语检查：CLI/报告文案仅用白话（点一下/输入文字/提示我/没找到/做完后没看到），
  技术词只出现在引擎内部日志/代码。

## 6. 波次2（进行中，2026-09-09 第一段完成）

已完成：
1. ✅ **E7 `engine/ai.py`**：智谱 glm-4.6v 适配（双图语义确认、归一化提示与解析、`AiGate`
   显式授权+知情提示钩子、无 key 抛 `ai_not_authorized`）；`SemanticStub` 确定性语义桩
   （区域化扫描：录点邻域圈 OCR 一次 + tokens 与候选词集统一匹配；整图行带滑窗兜底）
   ——离线测试/无网降级用。
2. ✅ **E5 `engine/calibrator.py`**：校验子流程（对接 executor 波次1 契约）：
   - `widget_not_found/click_guard_failed`（页面已锁定）：AI/语义桩粗圈 → 圈区本地
     精定位（旧词→短前缀→模板）→ 部件重采集 + 页内矩形/文字写回 → 写回前自检 →
     `updated=True`；改名恢复写回新文字（库存查询→库存中心 场景闭环）；
   - `page_not_found`：窗口矩形提供页面原点 → 语义粗圈 → 整窗重采集刷新
     page.image/size/rect/ts（anchors/context/scale_range 保留）→ 自检；
   - `first_run`：基线确认（有效 → 无写回；失效 → 自动升级恢复流程）；
   - 自检失败/AI 低置信/AI 缺席 → `ok=False` 旧值保留（人工兜底走 executor L1）；
   - 页面对象就地更新（`target.page` 与 `ctx.page_spec` 同引用自动同步）；
     executor 在 `updated` 时 bump 脚本 `targets_rev`（写回前旧值备份由 CLI/UI 负责）。
3. ✅ 引擎护栏改进：OCR 单次挂死 → 立即重建引擎自愈（短窗连续挂死才熔断冷却）；
   `find_text_ocr` 2× 放大只用于小字输入（区域高 <70px）；校准器 OCR 区域化改造
   （圈区/带一次 OCR + tokens 匹配，替代逐词多次 OCR——校准单次从 40–120s 降至 ~3s）。
4. ✅ CLI：`run --calibrate [--ai-cloud] [--save-on-calibrate]`（云端交互授权、写回先备份
   `.bak.json`；`calibrate_first_run` 随 --calibrate 开启）。
5. ✅ 测试：`test_ai.py`（解析/授权/桩）+ `test_calibrator.py`（小改版 guard 恢复、大改版
   整窗恢复、彻底改名保留旧值、first_run）→ **全套 97/97 全绿**。
6. ✅ **UIA ①级提供者**：`engine/uia.py`（树定位原语 + make_provider）；locator 同名按
   录点距离消歧、**整页容器盒拒绝（≥页面 70% 面积不采用）**、越页命中弃用；
   executor/LiveDriver 接线（`uia_provider()`/`window_rect()`）→ **全套 103/103 全绿**。
   真窗验证（Edge DOM，2026-09-09）：页面根控件（name=页面标题、盒≈整页）会先于
   真实按钮被匹配（141px 级偏差即 M0 记录的 Edge DOM 坐标偏差形态）→ 大盒防护 +
   录点距离 ≤60px 截断后本级弃用，正确降级 ②级 OCR（dev=1px），exists 语义不误报。
7. ✅ **真实窗口回归（2026-09-09 实跑）**：
   - `engine/scripts/migrate_m0_assets.py`：M0 目标迁移 → `.sgscript.json` v1.0 常驻用例
     （`engine/tests/assets/live/{login,erp,dyn}.sgscript.json`，页面整窗 1288×988 @125%
     内嵌 base64，Target 全量字段）；
   - `engine/scripts/live_regress.py`：Edge fixtures 真窗回归 + erp-v2 校准演示（结果登记
     `engine/scripts/live_regress.jsonl`）：
     | 场景 | 结果 |
     |---|---|
     | login 移窗 3 轮 | 3/3 通过，页面模板 0.987–0.993，部件 dev ≤1px |
     | erp 移窗 3 轮 | 3/3 通过，页面模板 0.993–0.997，部件 dev ≤2px |
     | dyn 动态心跳页 3 轮 | 3/3 通过，模板 ≥0.950（验收 ≥0.8），部件 dev ≤1px |
     | erp-v2 改版校准 | 首执 first_run 基线 + click_guard_failed → 自动重采集写回
       （text 库存查询→库存中心，targets_rev 0→1），落盘 JSON 可校验，复跑 dev=1px（≤20，无人工）|
     | **dyn_feed 强动态页锚恢复** | 内容流 fixture（主区每 1.2s 重排，整窗必失配）：
       首执基线 + page_not_found → 语义定位顶栏词 → 行带锚**跨帧复验 1.00** 写回
       page.anchors → 后续帧整窗失配时锚定位 dev=1px（≤20，无人工）|
     | 单步耗时（模板路径中位）| login 226ms / erp 252ms / dyn 239ms（≤500ms ✅）|
   - **bili 动态页锚恢复**：bili 客户端不在场，引擎能力已由 dyn_feed 真窗等价场景闭环
     （锚采集→跨帧复验→写回→锚定位 dev=1px）；真实客户端在场时的复核流程与
     `smoke/data/calib_bili_anchor.png` 资产留存 smoke/ 可复用。
   - 时序稳定性措施（记录）：Edge 前台激活/移动后需 ~2s 稳定等待；多窗共存时合成切换
     会引入定位噪声 → 回归逐 case 独占窗口 + 预热轮；窗口移动渲染未稳时轮内重试 ≤3 次。
   - **bili 真实客户端动态页锚恢复（复核完成，2026-09-09）**：哔哩哔哩客户端在场复核：
     旧 feed 整窗模板（bili_page.png）失配（score 0.51）→ 校验模式自动恢复：语义定位
     顶栏搜索框（placeholder 全词“搜索你感兴趣的视频”唯一化，feed 标题含短词“感兴趣”
     会误配——记录）→ 行带锚跨帧复验 1.00 写回 page.anchors + 整窗刷新 → 复验定位
     **dev=0px（≤20，无人工）**；点击仅聚焦搜索框无副作用，不关闭用户窗口。
     `live_regress.py --case bili` 常驻复核（bili 客户端在场时）。

待办（继续）：
- 低置信人工兜底的 Electron 弹窗（`calibrate.confirm`）→ UI 里程碑联调时立项；
- UI 里程碑联调：`HumanIO`/IPC 契约表（开发过程文档 §6.1）；真窗 bili 锚恢复（客户端在场时）。

## 7. M1 引擎验收对照（《M1_引擎设计清单》§5 八条）

| # | 验收条目 | 状态 | 证据 |
|---|---|---|---|
| 1 | 六动作×条件×循环端到端 | ✅ | test_executor（合成屏 37 项）+ CLI run 实跑 |
| 2 | 页面定位：0.8~1.25× 移窗/缩放命中；整窗失配→锚 | ✅（真窗部分） | live_regress login/erp/dyn dev≤2px；锚=locator 测试 + bili 延后 |
| 3 | 页面内约束（页面外相似物负例） | ✅ | test_locator 诱饵负例 + UIA 越页弃用 + 真窗 in_page 断言 |
| 4 | 校验模式：erp-v2 自动恢复 dev≤20px 无人工 | ✅（真窗） | live_regress erp_v2：updated=1、复跑 dev=1px |
| 5 | 首次执行进校验一次并写回（不改语义仅刷 Target） | ✅ | report.calib first_run + targets_rev bump + 落盘可校验 |
| 6 | 定位日志 JSONL + tail；结果性失败不触发校验 | ✅ | test_executor 日志契约 + on_fail 四策略（verify_failed 无 calib） |
| 7 | 单步部件定位 ≤500ms | ✅（模板路径真窗 226–293ms）| live_regress widget_tpl_ms；OCR 路径波动登记（文本索引缓存=M1 中段） |
| 8 | `.sgscript.json` 读写/校验；术语检查 | ✅ | test_schema/test_cli + 资产迁移 validate；CLI 运行面全白话（点一下/没找到/自动校准/做完后没看到…），
  validate/selftest 为开发者命令保留格式名与自检术语（口径：术语表约束“界面/用户日志/报错”）|

## 8. M1 引擎验收结论（波次2 收口，2026-09-09）

**结论：M1 引擎（E1–E8 模块 + 校验模式 + 静态锚 + 真窗回归资产）验收八条证据全部就绪
（§7 表），离线 105/105 全绿、真窗场景全绿（login/erp/dyn 移窗 dev≤2px、模板分
0.95–0.997；erp-v2 改版自动校准 dev=1px；dyn_feed 强动态页锚恢复 dev=1px；
**真实 bili 客户端动态页锚恢复 dev=0px**）。**

引擎侧验收口径说明与残余外部前置（不属引擎缺陷，登记跟踪）：
1. ~~真实 bili 客户端复核~~ **已完成**（2026-09-09：dev=0px，见 §6-7 条）。
2. **150% DPI / 跨分辨率整页缩放**：与《项目启动文档》§8.1 既有登记一致，属物理机补测
   项（多尺度 0.8~1.25× 已由合成测试覆盖、真窗 125% DPI 移窗 dev≤2px 验证）。
3. ~~**Electron UI / IPC 联调**（HumanIO、calibrate.confirm 弹窗、选区双截图）属 M1 里程碑
   的 UI 阶段任务~~ **已完成**（波次3，见 §9）：IPC 契约 v0.1 落地、Electron UI 可用、
   选中/运行/人工确认全链路联调通过；150% DPI 与"真人过一遍演示"仍为登记补测项（§9.6）。

回归入口（常驻）：
- 离线全套：`python -m engine.tests.run_all`（或 `run_watchdog.py` 逐测实时进度）
- 真窗回归与校准演示：`python engine/scripts/live_regress.py --case login,erp,dyn,erp_v2,dyn_feed`
- 资产重建：`python engine/scripts/migrate_m0_assets.py`
- 数据：`engine/scripts/live_regress.jsonl`、定位日志由 `--loc-log` 指定

## 9. 波次3：IPC + Electron UI + 联调（2026-09-10）

范围（用户拍板）：① 引擎 IPC 服务端 ② Electron 壳 + 列表编辑器 + 运行日志
③ 双截图选区覆盖层 ④ 校准/人工确认弹窗联调 ⑤ 零编程用户 5 分钟"自动登录"验收演示
⑥ 文档登记 + 提交。

### 9.1 交付清单

| 段 | 交付 | 状态 | 说明 |
|---|---|---|---|
| ① IPC | `engine/ipc.py` + `docs/M1_IPC契约.md` v0.1 | ✅ | stdio JSON-Lines、JSON-RPC 2.0 子集；方法 `ping/window.find/window.activate/script.new|load|save/page.capture/widget.capture/page.find/widget.locate/input.click|type|hotkey/script.run|stop/ai.authorize/confirm.reply/engine.shutdown`；事件 `event.log|step|run_state|run_done|calibrate|confirm_request|error`；错误码 -32700/-32601/-32602/4001/4002/4003 |
| ② UI | `app/`（Electron 33.4.11） | ✅ | 主进程 `EngineClient` 以 `python -m engine serve` 拉起引擎；列表式编辑器（六动作 + 如果看到/没看到 + 重复 N 次/一直重复 + 做完后应该看到 + 就做/否则/循环体插入位置 + 上移/下移/删除）；白话运行日志与状态；保存/打开 `.sgscript.json` |
| ③ 选区 | `app/overlay.html/js` | ✅ | 框页面 → 框部件两段式；Esc 取消；DIP↔物理按 `workArea` 原点 + `scaleFactor` 换算；**采集前先收起遮罩**（见 9.3-1） |
| ④ 联调 | `event.confirm_request` → `ui:confirm` → `confirm.reply` | ✅ | "提示我"/L1 没找到/预期结果失败三类弹窗；`event.calibrate` 映射白话日志 |
| ⑤ 演示 | `electron . --autotest-demo` | ✅ | 见 9.4：30.9 秒走完"搭脚本 → 运行 → 出错 → 加分支 → 再跑" |
| ⑥ 文档 | 本节 + 契约 v0.1 + 开发过程文档 §6.1 契约正本 | ✅ | 提交 `eb8539e`（IPC）、`69ed9ec`（UI） |

### 9.2 命令与入口

- 起引擎（供 UI 用）：`python -m engine serve`（stdio JSON-Lines）
- 起界面：`cd app && npm install && npx electron .`
- 三个自动冒烟：`npx electron . --autotest`（链路）/ `--autotest-pick`（选区）/
  `--autotest-demo`（零编程用户全流程）
- 引擎侧 IPC 测试：`python -m unittest discover -s engine/tests -p "test_ipc.py" -t engine/tests`

### 9.3 联调中发现并修掉的真问题（都带测试）

1. **选区遮罩压暗截图**：覆盖层是半透明全屏窗，第二步采集时它仍盖在屏幕上 →
   存进脚本的 page/target 图整体变暗（`page.find` 置信 0.978，小部件 OCR 直接读不出字）。
   修复：框完部件先 `overlay.hide()` 再 `page.capture` → 置信 **0.9996**、
   OCR 由空 → "登录"。
2. **小部件 OCR 被切成单字**：76×53 的按钮裁剪原图 OCR 得到 `['登','录']`，
   取最长项会写成"登"。修复：`matcher.ocr_run_auto`（高 <90px 先原图；认不出、超时
   或**只认出孤零零一个字**时 ×2~×3 放大再试，boxes 换算回原图）→ 得到 `['登录']`；
   `widget.capture` 改用它。
3. **无录点时页面下半区文字漏检**：文字兜底条带以页心为中心（±260px），
   贴近底部的提示（"密码错误"）搜不到。修复：无 `rect_in_page` 时追加
   `(页心 x, 0.78×页高)` 搜索点 → 演示中一次命中。
4. **fixture 陈旧副本**：临时目录里的演示页"只复制一次"，改了 fixture 仍跑到旧页面
   （白等一轮）。修复：`app/main.js` 与 `live_regress.copy_fixture` 每次都覆盖。
5. **界面容器下拉未接线**：`#insertInto` 选了"就做里面"但 `state.insertPath` 不更新，
   新步骤仍加到主流程。修复：补 `onchange`。
6. **运行前没把页面切到前台**：点按落在被遮挡的窗口上会点错地方（M0 已记录）。
   新增 IPC `window.activate` + `script.run` 运行前自动切前台（运行期间保持置顶、
   跑完自动降回；`activate:false` 可关）；窗口中心压着别的窗口时如实上报
   `occluded/top_title`，引擎记一条白话日志、UI 也能给出"被挡住了"的提示。
7. **同一位置的另一个 fixture 窗口压着目标页**：回归用 `--keep-windows` 留下的
   动态页窗口是置顶的，演示把它的画面当成了登录页（OCR 出来全是内容流词条）。
   除了上面第 6 条的遮挡上报，演示还加了"被挡住就直接报清楚、别硬跑"。

### 9.4 ⑤ 零编程用户"自动登录"验收演示（实测记录）

`npx electron . --autotest-demo`（合成鼠标框选 + 自动作答原生弹窗，其余全程走真实界面）：

| 时刻 | 发生了什么 |
|---|---|
| 0s | 引擎起来，找到登录页窗口（1275×975 物理 @125%），刷新到初始状态 |
| 9.1s | 框"请输入工号" → 界面回显 **已识别：请输入工号**（框 149×52） |
| 11.9s | 框"请输入密码" → **已识别：请输入密码**（150×51） |
| 14.2s | 框"登录" → **已识别：登录**（76×52） |
| 14.5s | 脚本：`输入文字(请输入工号)`、`输入文字(请输入密码)`、`点一下(登录)` |
| 17.9s | 第一次运行 **ok**（点击 1 次、输入 2 次）→ 页面上出现"密码错误，请重新输入" |
| 22.5s | 框线上那句红字 → 用界面加 `如果看到"密码错误，请重新输入"` → `就做里面` 加 `提示我"密码输错了，请重新输入"` |
| 23.7s | **术语检查通过**：界面可见文案 623 字，无技术词（DoD 5） |
| 24s | **保存/打开**：`.sgscript.json` 2.59MB，步骤 4 → 4，Target 图（data-url）内嵌完好（DoD 4） |
| 33.3s | 刷回初始状态后整脚本重跑：**ok**（点击 1 次、输入 2 次、提示 1 次），"提示我"弹窗 1 次、自动校准事件 2 次 |
| — | **总用时 33.3 秒**（DoD 口径：零编程用户 ≤5 分钟）→ `AUTOTEST-DEMO PASS` |

（首轮尝试记录：8.4s/11.2s/13.4s → 单步选区平均 ~2.6s，含引擎两次截图与 OCR。）

同时 `--autotest-pick`（选区专项）实测：`page.find` 置信 **0.9996**/298ms；
`widget.locate` 文字路径 `ocr_text` dev=8px（在框内）/230ms、模板路径 `tpl` **dev=1px**/178ms。

### 9.5 回归状态（波次3 收口）

- 离线全套：**140/140 全绿**（波次2 的 122 + IPC/窗口/OCR/定位新增 18）
- 真窗回归（2026-09-10 实跑）：login 4/4（模板 0.980–0.991，dev=1px）、
  erp 4/4（0.993–0.997，dev≤2px）、dyn 4/4（0.945–0.971，dev=1px）、
  erp_v2 自动校准 updated=1（库存查询→库存中心、targets_rev 0→1）、
  dyn_feed 锚恢复（页面重采集 + 跨帧复验 1.00 写回静态锚，复验 dev=3px）
- 单步部件定位（模板路径中位）：login 427ms / erp 232ms / dyn 293ms（≤500ms ✅）

### 9.6 登记待办（不阻塞本波次）

1. **重复运行同一脚本时的输入框目标**：录制时输入框是空的（占位符"请输入密码"），
   跑过一次后框内是内容/掩码 → 目标失配 → 走 L1"没找到"。演示里用"刷回初始状态"
   规避（用户重跑也会先回到登录页）。M2 项：录目标时优先取**静态边框**特征，
   或在首次执行校验里把"输入框类目标"的判别降级为"页面内位置 + 安全闸"。
2. **原生弹窗的真人点击**：自动化里 `ui:confirm` 走"自动作答"分支（同一条通道、
   引擎回复链路已验证）；`dialog.showMessageBox` 的实际按钮点击需真人过一遍。
3. **云端 AI 授权弹窗**：`kind=ai_authorize`（同意上传/使用本地识别）走同一通道；
   按 kind 定制白话文案与图标列为 UI 打磨项。
4. **150% DPI / 跨分辨率**：与启动文档 §8.1 登记一致，物理机补测。
5. 顶部标题栏/`Ctrl+S` 快捷键、撤销重做、节点图画布（不阻塞 MVP）未做。

## 10. M1 里程碑 DoD 对照（开发过程文档 §3，2026-09-10）

| # | DoD 条目 | 状态 | 证据 |
|---|---|---|---|
| 1 | 端到端可用：搭脚本 → 运行 → 失败提示全链路走通 | ✅ | `--autotest-demo` 全程走真实界面：选区→加步骤→运行→（点错/遮挡/没找到类失败都落到白话弹窗与 L1/L2）；`--autotest` 链路冒烟 |
| 2 | 零编程用户 ≤5 分钟搭出"自动登录"（含故意输错密码 → 触发"提示我"）并成功运行 | ✅（自动等价物 33.3 秒） | §9.4 表：三次框选 + 首跑 + 加分支 + 重跑，弹窗 1 次、校准 2 次；**真人过一遍见 §9.6-2** |
| 3 | 单步部件定位 ≤500ms | ✅ | 模板路径中位 232–427ms（真窗回归）；OCR 路径波动口径见 §7-7 |
| 4 | 脚本 JSON 可保存/加载，含 Target 全量字段 | ✅ | 界面"保存 → 打开"实测（2.59MB、步骤 4→4、Target 图 data-url 完好）+ `test_schema/test_cli/test_ipc` 往返校验 |
| 5 | 变更记录与术语检查通过（界面文案无技术词） | ✅ | §9 记录 + 演示内置术语检查（界面可见文案 623 字，黑名单 20 词零命中） |

**结论：M1 里程碑 DoD 五条证据齐备**；两项登记补测（真人过一遍演示 / 150% DPI 物理机）
不改变功能结论，见 §9.6。
